# `.gitignore`

## File identity

- Repository path: `.gitignore`
- Responsibility: Ignored generated material.
- Source SHA256: `30a170342d89d9cf46755b39b8e22e510caa3d7116232e7f64687d6a14855af6`
- Source SHA256 basis: `git-content`

Git consumes these patterns for **untracked** files. They ignore Python bytecode/virtual environments, `.env`, pytest/Ruff/LandScout caches, and generated contents of `data/raw`, `data/cache`, `data/processed` and `outputs`. Explicit negated rules retain each `.gitkeep` placeholder.

Ignore rules do not stop tracking an already committed file and are not a secrecy or deletion mechanism. The file performs no filesystem operation itself. Review exact staged paths to keep datasets, secrets and generated outputs out of a commit; changing ignore patterns does not authorize removing existing recovery material.

## Exact complete current file content

The fenced UTF-8 content below equals the Git-stored file bytes.

````text
# Python
__pycache__/
*.py[cod]
*.pyc
.venv/

# Environment files
.env

# Tool caches
.pytest_cache/
.ruff_cache/
.cache/landscout/

# Generated project data and outputs
data/raw/*
!data/raw/.gitkeep
data/cache/*
!data/cache/.gitkeep
data/processed/*
!data/processed/.gitkeep
outputs/*
!outputs/.gitkeep
````
