# `configs/planning/muret_bess_zoning_policy.yaml`

## File identity

- Repository path: `configs/planning/muret_bess_zoning_policy.yaml`
- File type: YAML configuration
- Primary responsibility: Defines the source-locked Muret written-zoning evidence occurrences, routes, chapter decisions, and applicability notes.
- Layer / domain: `checked-in configuration` / `planning`
- Public or internal role: Repository artifact; not a Python public API.
- Source SHA256: `879d50627c063bb10096950d004cf4d4e446ff04ef9a1178b3e3fb28e2ffdae3`

## 1. Purpose

Defines the source-locked Muret written-zoning evidence occurrences, routes, chapter decisions, and applicability notes.

## 2. Position in LandScout architecture

This `checked-in configuration` artifact supplies exact checked-in bytes to the current repository. Consumers found by exact path reference are: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`.

## 3. Imports and dependencies

Not a Python module. Its consumers parse or interpret the bytes using the source/configuration functions identified by repository references and pipeline documentation.

## 4. Constants and domains

Every parsed leaf field is listed below; list indices preserve source order.

| Field path | Exact checked-in value | Contract role |
|---|---|---|
| `schema_version` | `5` (`int`) | Selects the strict configuration schema; unsupported versions are rejected. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `policy_profile` | `"muret_bess_written_zoning_v6"` (`str`) | Identifies the versioned semantic policy snapshot. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `planning_precheck_scope` | `"WRITTEN_ZONING_REGULATION_ONLY"` (`str`) | Configures `planning precheck scope` under the exact parent path `<root>`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `review_scope` | `"CONFIGURED_USE_CONTROL_ARTICLES_ONLY"` (`str`) | Configures `review scope` under the exact parent path `<root>`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.document_id` | `"33edb4c9f6943c88d8d92518bff20bec"` (`str`) | Configures `document id` under the exact parent path `source_lock`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.archive_sha256` | `"9d6677cd6634b56b712311042f0cc714d5ca42a38f82a417b27dd473255d7d93"` (`str`) | Binds the exact bytes or canonical component named by `archive_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.pdf_sha256` | `"5358ebad6b0cda6de681ba3536e29b8b6291fb701c7d3711f4ee1d6fdb85c6fb"` (`str`) | Binds the exact bytes or canonical component named by `pdf_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.index_content_sha256` | `"6a0009228ca17128c0a8bb329d9c2277a1b6638708a67b913b72ee93063e42cd"` (`str`) | Binds the exact bytes or canonical component named by `index_content_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.structure_result_content_sha256` | `"16f8a9edfff0d330f69579310da085f804f4641de973d98e0046bff5ea96b03c"` (`str`) | Binds the exact bytes or canonical component named by `structure_result_content_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.structure_profile` | `"muret_plu_20240215_v1"` (`str`) | Configures `structure profile` under the exact parent path `source_lock`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `required_zone_article_numbers[0]` | `"1"` (`str`) | Ordered configured member of `required_zone_article_numbers`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `required_zone_article_numbers[1]` | `"2"` (`str`) | Ordered configured member of `required_zone_article_numbers`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].resolved_zone_chapter_label` | `"UA"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].reviewed_section_ids[0]` | `"SECTION-0008"` (`str`) | Ordered configured member of `chapters[0].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].reviewed_section_ids[1]` | `"SECTION-0009"` (`str`) | Ordered configured member of `chapters[0].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].review_note` | `"Articles UA 1 and UA 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].rationale` | `"Article UA 2 states a possible ICPE route and states separate compatibility and local-necessity conditions; whether a BESS qualifies remains unresolved."` (`str`) | Configures `rationale` under the exact parent path `chapters[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].missing_information` | `"BESS planning-use and ICPE classification, application of all Article UA 1/2 provisions, prescriptions, servitudes, project effects and design."` (`str`) | Configures `missing information` under the exact parent path `chapters[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].evidence_id` | `"MURET-UA-ICPE-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].section_id` | `"SECTION-0009"` (`str`) | Configures `section id` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].page_number` | `8` (`int`) | Configures `page number` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].exact_raw_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].excerpt_sha256` | `"e1c767bcf05e6e3879fda934afc396b55ecb8cb30b9be9d0e090c8ba860e13ff"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].section_page_fragment_sha256` | `"2da8d15fad096a694d7b56ecfc1d61d0ba375aac1c254794d63388524cc755f6"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].excerpt_start` | `100` (`int`) | Configures `excerpt start` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].excerpt_end` | `183` (`int`) | Configures `excerpt end` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].interpretation_note` | `"This is a literal ICPE route phrase; it does not establish that a BESS is an applicable ICPE use."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].source_rule_id` | `"MURET-UA-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition d’être compatibles avec le milieu environnant et nécessaires à la vie du \nquartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].source_rule_sha256` | `"8def59e860d434e482899e9709520d221dd576e41e00f276bbe9c87e5127a8df"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].source_rule_start` | `100` (`int`) | Configures `source rule start` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[0].source_rule_end` | `301` (`int`) | Configures `source rule end` under the exact parent path `chapters[0].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].evidence_id` | `"MURET-UA-ICPE-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].section_id` | `"SECTION-0009"` (`str`) | Configures `section id` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].page_number` | `8` (`int`) | Configures `page number` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].evidence_direction` | `"CONDITION"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].exact_raw_excerpt` | `"compatibles avec le milieu environnant et nécessaires à"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].excerpt_sha256` | `"45e7c822963aeb5931b00392f605e45f745de47c084d04490bff6bb15f46a928"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].section_page_fragment_sha256` | `"2da8d15fad096a694d7b56ecfc1d61d0ba375aac1c254794d63388524cc755f6"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].excerpt_start` | `210` (`int`) | Configures `excerpt start` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].excerpt_end` | `265` (`int`) | Configures `excerpt end` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].interpretation_note` | `"This is the separate compatibility and necessity qualification attached to the ICPE route."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].source_rule_id` | `"MURET-UA-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition d’être compatibles avec le milieu environnant et nécessaires à la vie du \nquartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].source_rule_sha256` | `"8def59e860d434e482899e9709520d221dd576e41e00f276bbe9c87e5127a8df"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].source_rule_start` | `100` (`int`) | Configures `source rule start` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].evidence[1].source_rule_end` | `301` (`int`) | Configures `source rule end` under the exact parent path `chapters[0].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].route_assessments[0].route_id` | `"MURET-UA-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[0].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].route_assessments[0].route_kind` | `"CONDITIONAL_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[0].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].route_assessments[0].positive_evidence_ids[0]` | `"MURET-UA-ICPE-ROUTE-01"` (`str`) | Ordered configured member of `chapters[0].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].route_assessments[0].condition_evidence_ids[0]` | `"MURET-UA-ICPE-CONDITION-01"` (`str`) | Ordered configured member of `chapters[0].route_assessments[0].condition_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].route_assessments[0].difficulty_evidence_ids` | `[]` (`list`) | Configures `difficulty evidence ids` under the exact parent path `chapters[0].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[0].route_assessments[0].applicability_note` | `"The cited positive category and its explicit qualification are assessed as one coherent route; BESS applicability remains unresolved."` (`str`) | Configures `applicability note` under the exact parent path `chapters[0].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].resolved_zone_chapter_label` | `"UB"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].reviewed_section_ids[0]` | `"SECTION-0021"` (`str`) | Ordered configured member of `chapters[1].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].reviewed_section_ids[1]` | `"SECTION-0022"` (`str`) | Ordered configured member of `chapters[1].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].review_note` | `"Articles UB 1 and UB 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].rationale` | `"Article UB 2 states a possible ICPE route and separate compatibility and local-necessity conditions; BESS applicability is unresolved."` (`str`) | Configures `rationale` under the exact parent path `chapters[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].missing_information` | `"BESS planning-use and ICPE classification, application of all Article UB 1/2 provisions, prescriptions, servitudes, project effects and design."` (`str`) | Configures `missing information` under the exact parent path `chapters[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].evidence_id` | `"MURET-UB-ICPE-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].section_id` | `"SECTION-0022"` (`str`) | Configures `section id` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].page_number` | `22` (`int`) | Configures `page number` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].exact_raw_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].excerpt_sha256` | `"e1c767bcf05e6e3879fda934afc396b55ecb8cb30b9be9d0e090c8ba860e13ff"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].section_page_fragment_sha256` | `"7c678bbc92c2271fbb02f0c228f51e0b408b862780731b78f301b37731a894f3"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].excerpt_start` | `98` (`int`) | Configures `excerpt start` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].excerpt_end` | `181` (`int`) | Configures `excerpt end` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].interpretation_note` | `"This is a literal ICPE route phrase, not a BESS authorization."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].source_rule_id` | `"MURET-UB-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].source_rule_start` | `98` (`int`) | Configures `source rule start` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[0].source_rule_end` | `307` (`int`) | Configures `source rule end` under the exact parent path `chapters[1].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].evidence_id` | `"MURET-UB-ICPE-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].section_id` | `"SECTION-0022"` (`str`) | Configures `section id` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].page_number` | `22` (`int`) | Configures `page number` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].evidence_direction` | `"CONDITION"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].exact_raw_excerpt` | `"compatibles avec le milieu environnant et nécessaires à"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].excerpt_sha256` | `"45e7c822963aeb5931b00392f605e45f745de47c084d04490bff6bb15f46a928"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].section_page_fragment_sha256` | `"7c678bbc92c2271fbb02f0c228f51e0b408b862780731b78f301b37731a894f3"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].excerpt_start` | `217` (`int`) | Configures `excerpt start` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].excerpt_end` | `272` (`int`) | Configures `excerpt end` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].interpretation_note` | `"This is the separate compatibility and necessity qualification."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].source_rule_id` | `"MURET-UB-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].source_rule_start` | `98` (`int`) | Configures `source rule start` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].evidence[1].source_rule_end` | `307` (`int`) | Configures `source rule end` under the exact parent path `chapters[1].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].route_assessments[0].route_id` | `"MURET-UB-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[1].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].route_assessments[0].route_kind` | `"CONDITIONAL_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[1].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].route_assessments[0].positive_evidence_ids[0]` | `"MURET-UB-ICPE-ROUTE-01"` (`str`) | Ordered configured member of `chapters[1].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].route_assessments[0].condition_evidence_ids[0]` | `"MURET-UB-ICPE-CONDITION-01"` (`str`) | Ordered configured member of `chapters[1].route_assessments[0].condition_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].route_assessments[0].difficulty_evidence_ids` | `[]` (`list`) | Configures `difficulty evidence ids` under the exact parent path `chapters[1].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[1].route_assessments[0].applicability_note` | `"The cited positive category and its explicit qualification are assessed as one coherent route; BESS applicability remains unresolved."` (`str`) | Configures `applicability note` under the exact parent path `chapters[1].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].resolved_zone_chapter_label` | `"UC"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].reviewed_section_ids[0]` | `"SECTION-0036"` (`str`) | Ordered configured member of `chapters[2].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].reviewed_section_ids[1]` | `"SECTION-0037"` (`str`) | Ordered configured member of `chapters[2].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].review_note` | `"Articles UC 1 and UC 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].rationale` | `"Article UC 2 states a possible ICPE route subject to explicit compatibility and local-necessity conditions; BESS applicability is unresolved."` (`str`) | Configures `rationale` under the exact parent path `chapters[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].missing_information` | `"BESS planning-use and ICPE classification, application of all Article UC 1/2 provisions, prescriptions, servitudes, project effects and design."` (`str`) | Configures `missing information` under the exact parent path `chapters[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].evidence_id` | `"MURET-UC-ICPE-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].section_id` | `"SECTION-0037"` (`str`) | Configures `section id` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].page_number` | `36` (`int`) | Configures `page number` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].exact_raw_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].excerpt_sha256` | `"e1c767bcf05e6e3879fda934afc396b55ecb8cb30b9be9d0e090c8ba860e13ff"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].section_page_fragment_sha256` | `"f6103c4139a65d12a9b6bf4c5edd37382fa6a2fa642c3a5805aa2898b1121365"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].excerpt_start` | `98` (`int`) | Configures `excerpt start` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].excerpt_end` | `181` (`int`) | Configures `excerpt end` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].interpretation_note` | `"This is a literal ICPE route phrase, not a BESS authorization."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].source_rule_id` | `"MURET-UC-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].source_rule_start` | `98` (`int`) | Configures `source rule start` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[0].source_rule_end` | `307` (`int`) | Configures `source rule end` under the exact parent path `chapters[2].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].evidence_id` | `"MURET-UC-ICPE-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].section_id` | `"SECTION-0037"` (`str`) | Configures `section id` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].page_number` | `36` (`int`) | Configures `page number` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].evidence_direction` | `"CONDITION"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].exact_raw_excerpt` | `"compatibles avec le milieu environnant et nécessaires à"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].excerpt_sha256` | `"45e7c822963aeb5931b00392f605e45f745de47c084d04490bff6bb15f46a928"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].section_page_fragment_sha256` | `"f6103c4139a65d12a9b6bf4c5edd37382fa6a2fa642c3a5805aa2898b1121365"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].excerpt_start` | `217` (`int`) | Configures `excerpt start` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].excerpt_end` | `272` (`int`) | Configures `excerpt end` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].interpretation_note` | `"This is the separate compatibility and necessity qualification."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].source_rule_id` | `"MURET-UC-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].source_rule_start` | `98` (`int`) | Configures `source rule start` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].evidence[1].source_rule_end` | `307` (`int`) | Configures `source rule end` under the exact parent path `chapters[2].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].route_assessments[0].route_id` | `"MURET-UC-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[2].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].route_assessments[0].route_kind` | `"CONDITIONAL_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[2].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].route_assessments[0].positive_evidence_ids[0]` | `"MURET-UC-ICPE-ROUTE-01"` (`str`) | Ordered configured member of `chapters[2].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].route_assessments[0].condition_evidence_ids[0]` | `"MURET-UC-ICPE-CONDITION-01"` (`str`) | Ordered configured member of `chapters[2].route_assessments[0].condition_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].route_assessments[0].difficulty_evidence_ids` | `[]` (`list`) | Configures `difficulty evidence ids` under the exact parent path `chapters[2].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[2].route_assessments[0].applicability_note` | `"The cited positive category and its explicit qualification are assessed as one coherent route; BESS applicability remains unresolved."` (`str`) | Configures `applicability note` under the exact parent path `chapters[2].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].resolved_zone_chapter_label` | `"UD"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].reviewed_section_ids[0]` | `"SECTION-0051"` (`str`) | Ordered configured member of `chapters[3].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].reviewed_section_ids[1]` | `"SECTION-0052"` (`str`) | Ordered configured member of `chapters[3].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].review_note` | `"Articles UD 1 and UD 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].rationale` | `"Article UD 2 states a possible ICPE route subject to explicit compatibility and local-necessity conditions; BESS applicability is unresolved."` (`str`) | Configures `rationale` under the exact parent path `chapters[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].missing_information` | `"BESS planning-use and ICPE classification, application of all Article UD 1/2 provisions, prescriptions, servitudes, project effects and design."` (`str`) | Configures `missing information` under the exact parent path `chapters[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].evidence_id` | `"MURET-UD-ICPE-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].section_id` | `"SECTION-0052"` (`str`) | Configures `section id` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].page_number` | `48` (`int`) | Configures `page number` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].exact_raw_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].excerpt_sha256` | `"e1c767bcf05e6e3879fda934afc396b55ecb8cb30b9be9d0e090c8ba860e13ff"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].section_page_fragment_sha256` | `"67701fcf91b57f6d4c00a0c26d95c2904e736bd70c3da1bd49b949f2d60f6e9a"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].excerpt_start` | `446` (`int`) | Configures `excerpt start` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].excerpt_end` | `529` (`int`) | Configures `excerpt end` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].interpretation_note` | `"This is a literal ICPE route phrase, not a BESS authorization."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].source_rule_id` | `"MURET-UD-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].source_rule_start` | `446` (`int`) | Configures `source rule start` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[0].source_rule_end` | `655` (`int`) | Configures `source rule end` under the exact parent path `chapters[3].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].evidence_id` | `"MURET-UD-ICPE-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].section_id` | `"SECTION-0052"` (`str`) | Configures `section id` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].page_number` | `48` (`int`) | Configures `page number` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].evidence_direction` | `"CONDITION"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].exact_raw_excerpt` | `"compatibles avec le milieu environnant et nécessaires à"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].excerpt_sha256` | `"45e7c822963aeb5931b00392f605e45f745de47c084d04490bff6bb15f46a928"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].section_page_fragment_sha256` | `"67701fcf91b57f6d4c00a0c26d95c2904e736bd70c3da1bd49b949f2d60f6e9a"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].excerpt_start` | `565` (`int`) | Configures `excerpt start` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].excerpt_end` | `620` (`int`) | Configures `excerpt end` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].interpretation_note` | `"This is the separate compatibility and necessity qualification."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].source_rule_id` | `"MURET-UD-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].source_rule_start` | `446` (`int`) | Configures `source rule start` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].evidence[1].source_rule_end` | `655` (`int`) | Configures `source rule end` under the exact parent path `chapters[3].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].route_assessments[0].route_id` | `"MURET-UD-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[3].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].route_assessments[0].route_kind` | `"CONDITIONAL_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[3].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].route_assessments[0].positive_evidence_ids[0]` | `"MURET-UD-ICPE-ROUTE-01"` (`str`) | Ordered configured member of `chapters[3].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].route_assessments[0].condition_evidence_ids[0]` | `"MURET-UD-ICPE-CONDITION-01"` (`str`) | Ordered configured member of `chapters[3].route_assessments[0].condition_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].route_assessments[0].difficulty_evidence_ids` | `[]` (`list`) | Configures `difficulty evidence ids` under the exact parent path `chapters[3].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[3].route_assessments[0].applicability_note` | `"The cited positive category and its explicit qualification are assessed as one coherent route; BESS applicability remains unresolved."` (`str`) | Configures `applicability note` under the exact parent path `chapters[3].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].resolved_zone_chapter_label` | `"UF"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].reviewed_section_ids[0]` | `"SECTION-0065"` (`str`) | Ordered configured member of `chapters[4].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].reviewed_section_ids[1]` | `"SECTION-0066"` (`str`) | Ordered configured member of `chapters[4].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].review_note` | `"Articles UF 1 and UF 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].rationale` | `"Article UF 2 states a possible ICPE route subject to explicit compatibility and local-necessity conditions; sector and BESS applicability remain unresolved."` (`str`) | Configures `rationale` under the exact parent path `chapters[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].missing_information` | `"BESS planning-use, sector and ICPE classification, application of all Article UF 1/2 provisions, prescriptions, servitudes, project effects and design."` (`str`) | Configures `missing information` under the exact parent path `chapters[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].evidence_id` | `"MURET-UF-ICPE-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].section_id` | `"SECTION-0066"` (`str`) | Configures `section id` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].page_number` | `60` (`int`) | Configures `page number` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].exact_raw_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].excerpt_sha256` | `"e1c767bcf05e6e3879fda934afc396b55ecb8cb30b9be9d0e090c8ba860e13ff"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].section_page_fragment_sha256` | `"4fceabfce9821f94b0c023052a654d1d515c86e81056605b876cfbccf54e84ec"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].excerpt_start` | `510` (`int`) | Configures `excerpt start` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].excerpt_end` | `593` (`int`) | Configures `excerpt end` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].interpretation_note` | `"This is a literal ICPE route phrase, not a BESS authorization."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].source_rule_id` | `"MURET-UF-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].source_rule_start` | `510` (`int`) | Configures `source rule start` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[0].source_rule_end` | `719` (`int`) | Configures `source rule end` under the exact parent path `chapters[4].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].evidence_id` | `"MURET-UF-ICPE-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].section_id` | `"SECTION-0066"` (`str`) | Configures `section id` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].page_number` | `60` (`int`) | Configures `page number` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].evidence_direction` | `"CONDITION"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].exact_raw_excerpt` | `"compatibles avec le milieu environnant et nécessaires à"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].excerpt_sha256` | `"45e7c822963aeb5931b00392f605e45f745de47c084d04490bff6bb15f46a928"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].section_page_fragment_sha256` | `"4fceabfce9821f94b0c023052a654d1d515c86e81056605b876cfbccf54e84ec"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].excerpt_start` | `629` (`int`) | Configures `excerpt start` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].excerpt_end` | `684` (`int`) | Configures `excerpt end` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].interpretation_note` | `"This is the separate compatibility and necessity qualification."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].source_rule_id` | `"MURET-UF-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].source_rule_start` | `510` (`int`) | Configures `source rule start` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].evidence[1].source_rule_end` | `719` (`int`) | Configures `source rule end` under the exact parent path `chapters[4].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].route_assessments[0].route_id` | `"MURET-UF-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[4].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].route_assessments[0].route_kind` | `"CONDITIONAL_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[4].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].route_assessments[0].positive_evidence_ids[0]` | `"MURET-UF-ICPE-ROUTE-01"` (`str`) | Ordered configured member of `chapters[4].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].route_assessments[0].condition_evidence_ids[0]` | `"MURET-UF-ICPE-CONDITION-01"` (`str`) | Ordered configured member of `chapters[4].route_assessments[0].condition_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].route_assessments[0].difficulty_evidence_ids` | `[]` (`list`) | Configures `difficulty evidence ids` under the exact parent path `chapters[4].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[4].route_assessments[0].applicability_note` | `"The cited positive category and its explicit qualification are assessed as one coherent route; BESS applicability remains unresolved."` (`str`) | Configures `applicability note` under the exact parent path `chapters[4].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].resolved_zone_chapter_label` | `"UP"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].reviewed_section_ids[0]` | `"SECTION-0080"` (`str`) | Ordered configured member of `chapters[5].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].reviewed_section_ids[1]` | `"SECTION-0081"` (`str`) | Ordered configured member of `chapters[5].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].review_note` | `"Articles UP 1 and UP 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].rationale` | `"Article UP 1 states a general restriction with a public or collective-interest equipment exception; whether a BESS belongs to that excepted category remains unresolved. The separate Article UP 2 ICPE rule is retained only as context because BESS ICPE applicability has not been established."` (`str`) | Configures `rationale` under the exact parent path `chapters[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].missing_information` | `"Formal classification as public or collective-interest equipment, BESS ICPE applicability, all Article UP 1/2 provisions, prescriptions, servitudes, project effects and design."` (`str`) | Configures `missing information` under the exact parent path `chapters[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].evidence_id` | `"MURET-UP-PUBLIC-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].section_id` | `"SECTION-0080"` (`str`) | Configures `section id` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].page_number` | `71` (`int`) | Configures `page number` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].evidence_kind` | `"PUBLIC_INTEREST_EXCEPTION"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].exact_raw_excerpt` | `"à usage d'équipement public \nou d'intérêt collectif"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].excerpt_sha256` | `"301da057642435982e74e393d12e292b81682d4d7672dec60e40a8e10e84530c"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].section_page_fragment_sha256` | `"06f8ea334a2fa8ce62337d6a3c59d24e03f9d8b9d8cc9e936c92e97b771babbb"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].excerpt_start` | `125` (`int`) | Configures `excerpt start` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].excerpt_end` | `177` (`int`) | Configures `excerpt end` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].interpretation_note` | `"This is the exact category exception; the policy does not decide that a BESS belongs to it."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].source_rule_id` | `"MURET-UP-ROUTE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].source_rule_excerpt` | `"Toutes constructions ou installations autres que celles à usage d'équipement public \nou d'intérêt collectif, services annexes et les logements de fonction y afférent."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].source_rule_sha256` | `"de2615e25b83708c84e9ff9313060dca708ca0a8bc693777b627951bc2de394c"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].source_rule_start` | `68` (`int`) | Configures `source rule start` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[0].source_rule_end` | `236` (`int`) | Configures `source rule end` under the exact parent path `chapters[5].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].evidence_id` | `"MURET-UP-RESTRICTION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].section_id` | `"SECTION-0080"` (`str`) | Configures `section id` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].page_number` | `71` (`int`) | Configures `page number` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].evidence_kind` | `"USE_RESTRICTION"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].evidence_direction` | `"SUPPORTS_DIFFICULTY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].exact_raw_excerpt` | `"Toutes constructions ou installations autres que celles"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].excerpt_sha256` | `"edfbe54799b8a6c0e74d86b0e9596e8c68471f11105783b3e4e93825f8308462"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].section_page_fragment_sha256` | `"06f8ea334a2fa8ce62337d6a3c59d24e03f9d8b9d8cc9e936c92e97b771babbb"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].excerpt_start` | `68` (`int`) | Configures `excerpt start` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].excerpt_end` | `124` (`int`) | Configures `excerpt end` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].interpretation_note` | `"This is the general restriction surrounding the public or collective-interest exception; it does not decide whether a BESS belongs to the exception."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].source_rule_id` | `"MURET-UP-ROUTE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].source_rule_excerpt` | `"Toutes constructions ou installations autres que celles à usage d'équipement public \nou d'intérêt collectif, services annexes et les logements de fonction y afférent."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].source_rule_sha256` | `"de2615e25b83708c84e9ff9313060dca708ca0a8bc693777b627951bc2de394c"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].source_rule_start` | `68` (`int`) | Configures `source rule start` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[1].source_rule_end` | `236` (`int`) | Configures `source rule end` under the exact parent path `chapters[5].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].evidence_id` | `"MURET-UP-ICPE-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].section_id` | `"SECTION-0081"` (`str`) | Configures `section id` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].page_number` | `71` (`int`) | Configures `page number` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].evidence_direction` | `"CONTEXT_ONLY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].exact_raw_excerpt` | `"compatibles avec le milieu environnant et nécessaires à"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].excerpt_sha256` | `"45e7c822963aeb5931b00392f605e45f745de47c084d04490bff6bb15f46a928"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].section_page_fragment_sha256` | `"7a5fac0b06f32a02a34031e9db62b2ccd59a63099fdb378079ab41c4252aed09"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].excerpt_start` | `478` (`int`) | Configures `excerpt start` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].excerpt_end` | `533` (`int`) | Configures `excerpt end` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].interpretation_note` | `"This separate ICPE condition is context only unless a future evidence step establishes that the BESS project is subject to it."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].source_rule_id` | `"MURET-UP-CONDITION-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].source_rule_start` | `359` (`int`) | Configures `source rule start` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].evidence[2].source_rule_end` | `568` (`int`) | Configures `source rule end` under the exact parent path `chapters[5].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].route_assessments[0].route_id` | `"MURET-UP-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[5].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].route_assessments[0].route_kind` | `"RESTRICTION_EXCEPTION_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[5].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].route_assessments[0].positive_evidence_ids[0]` | `"MURET-UP-PUBLIC-ROUTE-01"` (`str`) | Ordered configured member of `chapters[5].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].route_assessments[0].condition_evidence_ids` | `[]` (`list`) | Configures `condition evidence ids` under the exact parent path `chapters[5].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].route_assessments[0].difficulty_evidence_ids[0]` | `"MURET-UP-RESTRICTION-01"` (`str`) | Ordered configured member of `chapters[5].route_assessments[0].difficulty_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[5].route_assessments[0].applicability_note` | `"The Article UP 1 restriction and its public or collective-interest exception are assessed as one coherent route; BESS membership remains unresolved. The separate ICPE rule is not used to qualify this route."` (`str`) | Configures `applicability note` under the exact parent path `chapters[5].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].resolved_zone_chapter_label` | `"AU"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].reviewed_section_ids[0]` | `"SECTION-0095"` (`str`) | Ordered configured member of `chapters[6].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].reviewed_section_ids[1]` | `"SECTION-0096"` (`str`) | Ordered configured member of `chapters[6].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].review_note` | `"Articles AU 1 and AU 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].rationale` | `"Infrastructure prerequisites were not treated as a route; Article AU 2 separately states a possible ICPE route with compatibility and necessity conditions."` (`str`) | Configures `rationale` under the exact parent path `chapters[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].missing_information` | `"BESS planning-use and ICPE classification, infrastructure and sector conditions, all Article AU 1/2 provisions, prescriptions, servitudes, project effects and design."` (`str`) | Configures `missing information` under the exact parent path `chapters[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].evidence_id` | `"MURET-AU-ICPE-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].section_id` | `"SECTION-0096"` (`str`) | Configures `section id` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].page_number` | `81` (`int`) | Configures `page number` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].exact_raw_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].excerpt_sha256` | `"e1c767bcf05e6e3879fda934afc396b55ecb8cb30b9be9d0e090c8ba860e13ff"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].section_page_fragment_sha256` | `"545168e51a47f7c8b9519575b6d870ab70e11d1043df847e3b5b8661a890652e"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].excerpt_start` | `1474` (`int`) | Configures `excerpt start` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].excerpt_end` | `1557` (`int`) | Configures `excerpt end` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].interpretation_note` | `"This is the explicit ICPE route phrase; infrastructure prerequisites alone were not used as positive evidence."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].source_rule_id` | `"MURET-AU-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].source_rule_start` | `1474` (`int`) | Configures `source rule start` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[0].source_rule_end` | `1683` (`int`) | Configures `source rule end` under the exact parent path `chapters[6].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].evidence_id` | `"MURET-AU-ICPE-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].section_id` | `"SECTION-0096"` (`str`) | Configures `section id` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].page_number` | `81` (`int`) | Configures `page number` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].evidence_direction` | `"CONDITION"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].exact_raw_excerpt` | `"compatibles avec le milieu environnant et nécessaires à"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].excerpt_sha256` | `"45e7c822963aeb5931b00392f605e45f745de47c084d04490bff6bb15f46a928"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].section_page_fragment_sha256` | `"545168e51a47f7c8b9519575b6d870ab70e11d1043df847e3b5b8661a890652e"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].excerpt_start` | `1593` (`int`) | Configures `excerpt start` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].excerpt_end` | `1648` (`int`) | Configures `excerpt end` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].interpretation_note` | `"This is the separate compatibility and necessity qualification."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].source_rule_id` | `"MURET-AU-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].source_rule_start` | `1474` (`int`) | Configures `source rule start` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].evidence[1].source_rule_end` | `1683` (`int`) | Configures `source rule end` under the exact parent path `chapters[6].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].route_assessments[0].route_id` | `"MURET-AU-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[6].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].route_assessments[0].route_kind` | `"CONDITIONAL_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[6].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].route_assessments[0].positive_evidence_ids[0]` | `"MURET-AU-ICPE-ROUTE-01"` (`str`) | Ordered configured member of `chapters[6].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].route_assessments[0].condition_evidence_ids[0]` | `"MURET-AU-ICPE-CONDITION-01"` (`str`) | Ordered configured member of `chapters[6].route_assessments[0].condition_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].route_assessments[0].difficulty_evidence_ids` | `[]` (`list`) | Configures `difficulty evidence ids` under the exact parent path `chapters[6].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[6].route_assessments[0].applicability_note` | `"The cited positive category and its explicit qualification are assessed as one coherent route; BESS applicability remains unresolved."` (`str`) | Configures `applicability note` under the exact parent path `chapters[6].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].resolved_zone_chapter_label` | `"AUp"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].reviewed_section_ids[0]` | `"SECTION-0110"` (`str`) | Ordered configured member of `chapters[7].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].reviewed_section_ids[1]` | `"SECTION-0111"` (`str`) | Ordered configured member of `chapters[7].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].review_note` | `"Articles AUp 1 and AUp 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].rationale` | `"Article AUp 1 states a public or collective-interest equipment exception under Article AUp 2 conditions. Article AUp 2 requires indispensable access, road and network infrastructure before authorization. Its separate ICPE rule is retained only as context because BESS ICPE applicability has not been established."` (`str`) | Configures `rationale` under the exact parent path `chapters[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].missing_information` | `"Formal BESS classification as public or collective-interest equipment, satisfaction of the Article AUp 2 infrastructure prerequisite, BESS ICPE applicability, all Article AUp 1/2 provisions, prescriptions, servitudes, project effects and design."` (`str`) | Configures `missing information` under the exact parent path `chapters[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].evidence_id` | `"MURET-AUP-PUBLIC-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].section_id` | `"SECTION-0110"` (`str`) | Configures `section id` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].page_number` | `93` (`int`) | Configures `page number` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].evidence_kind` | `"PUBLIC_INTEREST_EXCEPTION"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].exact_raw_excerpt` | `"à usage d'équipement public ou \nd'intérêt collectif"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].excerpt_sha256` | `"f7be71b131f97c74c8107bc6f14bf2a980d8c3f769a52eef7a899249108c35a2"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].section_page_fragment_sha256` | `"4f5b79666858745347ec811398acd19d2761705b3b3d2a31ffd9f4c54a5c93d5"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].excerpt_start` | `125` (`int`) | Configures `excerpt start` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].excerpt_end` | `176` (`int`) | Configures `excerpt end` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].interpretation_note` | `"This is the exact category exception; BESS membership is unresolved."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].source_rule_id` | `"MURET-AUp-ROUTE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].source_rule_excerpt` | `"Toutes constructions ou installations autres que celles à usage d'équipement public ou \nd'intérêt collectif, leurs services annexes et les logements de fonction y afférent sous \nconditions de l’article AUP-2."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].source_rule_sha256` | `"01870b2aa63b15491cbf644501dfa8238a94f980d426d15ee2743cc5796c24c3"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].source_rule_start` | `69` (`int`) | Configures `source rule start` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[0].source_rule_end` | `278` (`int`) | Configures `source rule end` under the exact parent path `chapters[7].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].evidence_id` | `"MURET-AUP-INFRASTRUCTURE-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].section_id` | `"SECTION-0111"` (`str`) | Configures `section id` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].page_number` | `93` (`int`) | Configures `page number` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].evidence_kind` | `"ACCESS_OR_NETWORK_CONDITION"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].evidence_direction` | `"CONDITION"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].exact_raw_excerpt` | `"Les constructions et opérations ne pourront être autorisées qu’après réalisation des \néquipements d’infrastructure indispensable à leur fonctionnement (accès, voirie et \nréseaux divers) conformément aux articles AUp3 et AUp4."` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].excerpt_sha256` | `"b2be9b1f7e3597802d5ed2c301a7e34bb7a9eecaeab55898e55306719b1b315b"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].section_page_fragment_sha256` | `"57540d28148aefc320fcc8baa9a92df7e382d72299da6e804a3ebfaf52408b44"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].excerpt_start` | `98` (`int`) | Configures `excerpt start` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].excerpt_end` | `325` (`int`) | Configures `excerpt end` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].interpretation_note` | `"This is the general Article AUp 2 infrastructure prerequisite expressly referenced by Article AUp 1; the policy does not decide that it is satisfied."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].source_rule_id` | `"MURET-AUp-INFRASTRUCTURE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].source_rule_excerpt` | `"Les constructions et opérations ne pourront être autorisées qu’après réalisation des \néquipements d’infrastructure indispensable à leur fonctionnement (accès, voirie et \nréseaux divers) conformément aux articles AUp3 et AUp4."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].source_rule_sha256` | `"b2be9b1f7e3597802d5ed2c301a7e34bb7a9eecaeab55898e55306719b1b315b"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].source_rule_start` | `98` (`int`) | Configures `source rule start` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[1].source_rule_end` | `325` (`int`) | Configures `source rule end` under the exact parent path `chapters[7].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].evidence_id` | `"MURET-AUP-ICPE-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].section_id` | `"SECTION-0111"` (`str`) | Configures `section id` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].page_number` | `93` (`int`) | Configures `page number` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].evidence_direction` | `"CONTEXT_ONLY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].exact_raw_excerpt` | `"compatibles avec le milieu environnant et nécessaires à"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].excerpt_sha256` | `"45e7c822963aeb5931b00392f605e45f745de47c084d04490bff6bb15f46a928"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].section_page_fragment_sha256` | `"57540d28148aefc320fcc8baa9a92df7e382d72299da6e804a3ebfaf52408b44"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].excerpt_start` | `713` (`int`) | Configures `excerpt start` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].excerpt_end` | `768` (`int`) | Configures `excerpt end` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].interpretation_note` | `"This separate ICPE condition is context only unless a future evidence step establishes that the BESS project is subject to it."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].source_rule_id` | `"MURET-AUp-CONDITION-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].source_rule_start` | `594` (`int`) | Configures `source rule start` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].evidence[2].source_rule_end` | `803` (`int`) | Configures `source rule end` under the exact parent path `chapters[7].evidence[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].route_assessments[0].route_id` | `"MURET-AUp-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[7].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].route_assessments[0].route_kind` | `"CONDITIONAL_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[7].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].route_assessments[0].positive_evidence_ids[0]` | `"MURET-AUP-PUBLIC-ROUTE-01"` (`str`) | Ordered configured member of `chapters[7].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].route_assessments[0].condition_evidence_ids[0]` | `"MURET-AUP-INFRASTRUCTURE-CONDITION-01"` (`str`) | Ordered configured member of `chapters[7].route_assessments[0].condition_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].route_assessments[0].difficulty_evidence_ids` | `[]` (`list`) | Configures `difficulty evidence ids` under the exact parent path `chapters[7].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[7].route_assessments[0].applicability_note` | `"The Article AUp 1 public or collective-interest route is assessed with the general Article AUp 2 infrastructure prerequisite. BESS category membership and satisfaction remain unresolved; the separate ICPE rule does not qualify this route unless independently applicable."` (`str`) | Configures `applicability note` under the exact parent path `chapters[7].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].resolved_zone_chapter_label` | `"AUf"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].reviewed_section_ids[0]` | `"SECTION-0125"` (`str`) | Ordered configured member of `chapters[8].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].reviewed_section_ids[1]` | `"SECTION-0126"` (`str`) | Ordered configured member of `chapters[8].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].review_note` | `"Articles AUf 1 and AUf 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].rationale` | `"Infrastructure prerequisites were not treated as route evidence; Article AUf 2 separately states a possible ICPE route with compatibility and necessity conditions."` (`str`) | Configures `rationale` under the exact parent path `chapters[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].missing_information` | `"BESS planning-use, sector and ICPE classification, infrastructure and orientation requirements, all Article AUf 1/2 provisions, prescriptions and project design."` (`str`) | Configures `missing information` under the exact parent path `chapters[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].evidence_id` | `"MURET-AUF-ICPE-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].section_id` | `"SECTION-0126"` (`str`) | Configures `section id` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].page_number` | `102` (`int`) | Configures `page number` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].exact_raw_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].excerpt_sha256` | `"e1c767bcf05e6e3879fda934afc396b55ecb8cb30b9be9d0e090c8ba860e13ff"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].section_page_fragment_sha256` | `"ef0d2718332307afa871176c64cb8627900702dbd583819fb765adb2d1902769"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].excerpt_start` | `1435` (`int`) | Configures `excerpt start` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].excerpt_end` | `1518` (`int`) | Configures `excerpt end` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].interpretation_note` | `"This is the explicit ICPE route phrase; infrastructure prerequisites alone were not used as positive evidence."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].source_rule_id` | `"MURET-AUf-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].source_rule_start` | `1435` (`int`) | Configures `source rule start` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[0].source_rule_end` | `1644` (`int`) | Configures `source rule end` under the exact parent path `chapters[8].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].evidence_id` | `"MURET-AUF-ICPE-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].section_id` | `"SECTION-0126"` (`str`) | Configures `section id` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].page_number` | `102` (`int`) | Configures `page number` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].evidence_kind` | `"ICPE_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].evidence_direction` | `"CONDITION"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].exact_raw_excerpt` | `"compatibles avec le milieu environnant et nécessaires à"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].excerpt_sha256` | `"45e7c822963aeb5931b00392f605e45f745de47c084d04490bff6bb15f46a928"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].section_page_fragment_sha256` | `"ef0d2718332307afa871176c64cb8627900702dbd583819fb765adb2d1902769"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].excerpt_start` | `1554` (`int`) | Configures `excerpt start` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].excerpt_end` | `1609` (`int`) | Configures `excerpt end` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].interpretation_note` | `"This is the separate compatibility and necessity qualification."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].source_rule_id` | `"MURET-AUf-ICPE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].source_rule_excerpt` | `"Les installations classées pour la protection de l’environnement ne sont autorisées qu’à \nla condition qu'elles soient compatibles avec le milieu environnant et nécessaires à la \nvie du quartier et de la cité."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].source_rule_sha256` | `"890d2eab01e088948e78ff964bf68bc046d4bf88894f19ae20883793e5abb716"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].source_rule_start` | `1435` (`int`) | Configures `source rule start` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].evidence[1].source_rule_end` | `1644` (`int`) | Configures `source rule end` under the exact parent path `chapters[8].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].route_assessments[0].route_id` | `"MURET-AUf-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[8].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].route_assessments[0].route_kind` | `"CONDITIONAL_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[8].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].route_assessments[0].positive_evidence_ids[0]` | `"MURET-AUF-ICPE-ROUTE-01"` (`str`) | Ordered configured member of `chapters[8].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].route_assessments[0].condition_evidence_ids[0]` | `"MURET-AUF-ICPE-CONDITION-01"` (`str`) | Ordered configured member of `chapters[8].route_assessments[0].condition_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].route_assessments[0].difficulty_evidence_ids` | `[]` (`list`) | Configures `difficulty evidence ids` under the exact parent path `chapters[8].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[8].route_assessments[0].applicability_note` | `"The cited positive category and its explicit qualification are assessed as one coherent route; BESS applicability remains unresolved."` (`str`) | Configures `applicability note` under the exact parent path `chapters[8].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].resolved_zone_chapter_label` | `"AU0"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].reviewed_section_ids[0]` | `"SECTION-0140"` (`str`) | Ordered configured member of `chapters[9].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].reviewed_section_ids[1]` | `"SECTION-0141"` (`str`) | Ordered configured member of `chapters[9].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].review_note` | `"Articles AU0 1 and AU0 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].rationale` | `"Article AU0 1 identifies an exception for collective-interest networks and public infrastructure, while Article AU0 2 states a separate PLU-modification prerequisite for new construction or operations."` (`str`) | Configures `rationale` under the exact parent path `chapters[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].missing_information` | `"Formal BESS classification within the stated infrastructure exception, applicability of the modification prerequisite, all Article AU0 1/2 provisions, prescriptions and project design."` (`str`) | Configures `missing information` under the exact parent path `chapters[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].evidence_id` | `"MURET-AU0-INFRA-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].section_id` | `"SECTION-0140"` (`str`) | Configures `section id` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].page_number` | `114` (`int`) | Configures `page number` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].evidence_kind` | `"TECHNICAL_EQUIPMENT_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].exact_raw_excerpt` | `"installations nécessaires aux réseaux \nd’intérêt collectif, aux ouvrages publics d’infrastructures"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].excerpt_sha256` | `"886aaceafb2a40e73e3ebe145b3a58b6a22b239a8ce5fcc3740cb99d7d6298a0"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].section_page_fragment_sha256` | `"2f4cf931c76c5a5a29aa69d67b7986b092aff39ea13e4b302513177e5fef6619"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].excerpt_start` | `119` (`int`) | Configures `excerpt start` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].excerpt_end` | `217` (`int`) | Configures `excerpt end` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].interpretation_note` | `"This is an exact infrastructure exception; BESS qualification is unresolved."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].source_rule_id` | `"MURET-AU0-ROUTE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].source_rule_excerpt` | `"Sont interdites toutes les constructions autres que les installations nécessaires aux réseaux \nd’intérêt collectif, aux ouvrages publics d’infrastructures, et les extensions définies à \nl’article AU0 – 2."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].source_rule_sha256` | `"fa20142522483be8183df1a43e069fcb350b1de83347bdfdd733f7871bcf207d"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].source_rule_start` | `63` (`int`) | Configures `source rule start` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[0].source_rule_end` | `267` (`int`) | Configures `source rule end` under the exact parent path `chapters[9].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].evidence_id` | `"MURET-AU0-MODIFICATION-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].section_id` | `"SECTION-0141"` (`str`) | Configures `section id` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].page_number` | `114` (`int`) | Configures `page number` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].evidence_kind` | `"OTHER_RELEVANT_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].evidence_direction` | `"CONDITION"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].exact_raw_excerpt` | `"Les constructions et opérations nouvelles ne pourront être autorisées qu’après la \nmise en œuvre d’une procédure de modification du PLU"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].excerpt_sha256` | `"01594e632fb064a7e4dd408e68c156b66711abe6ae2a0e470c779e94b91f3a48"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].section_page_fragment_sha256` | `"a239cf9eff040ca9c9ab608cf040e3c739d5111c877aa68ad1773d3adadf24a5"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].excerpt_start` | `99` (`int`) | Configures `excerpt start` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].excerpt_end` | `234` (`int`) | Configures `excerpt end` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].interpretation_note` | `"This prerequisite is a condition only and is not treated as evidence that a BESS route exists."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].source_rule_id` | `"MURET-AU0-CONDITION-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].source_rule_excerpt` | `"Les constructions et opérations nouvelles ne pourront être autorisées qu’après la \nmise en œuvre d’une procédure de modification du PLU."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].source_rule_sha256` | `"2d9633774f414a8ad2f8e42bfcbb2507b677906ca6aca480f0239cec007942e3"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].source_rule_start` | `99` (`int`) | Configures `source rule start` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].evidence[1].source_rule_end` | `235` (`int`) | Configures `source rule end` under the exact parent path `chapters[9].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].route_assessments[0].route_id` | `"MURET-AU0-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[9].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].route_assessments[0].route_kind` | `"CONDITIONAL_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[9].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].route_assessments[0].positive_evidence_ids[0]` | `"MURET-AU0-INFRA-ROUTE-01"` (`str`) | Ordered configured member of `chapters[9].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].route_assessments[0].condition_evidence_ids[0]` | `"MURET-AU0-MODIFICATION-CONDITION-01"` (`str`) | Ordered configured member of `chapters[9].route_assessments[0].condition_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].route_assessments[0].difficulty_evidence_ids` | `[]` (`list`) | Configures `difficulty evidence ids` under the exact parent path `chapters[9].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[9].route_assessments[0].applicability_note` | `"The cited positive category and its explicit qualification are assessed as one coherent route; BESS applicability remains unresolved."` (`str`) | Configures `applicability note` under the exact parent path `chapters[9].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].resolved_zone_chapter_label` | `"AUf0"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].reviewed_section_ids[0]` | `"SECTION-0155"` (`str`) | Ordered configured member of `chapters[10].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].reviewed_section_ids[1]` | `"SECTION-0156"` (`str`) | Ordered configured member of `chapters[10].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].review_note` | `"Articles AUf0 1 and AUf0 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].rationale` | `"Article AUf0 1 identifies an exception for collective-interest networks and public infrastructure, while Article AUf0 2 states a separate PLU-modification prerequisite."` (`str`) | Configures `rationale` under the exact parent path `chapters[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].missing_information` | `"Formal BESS classification within the infrastructure exception, applicability of the modification prerequisite, all Article AUf0 1/2 provisions, prescriptions and project design."` (`str`) | Configures `missing information` under the exact parent path `chapters[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].evidence_id` | `"MURET-AUF0-INFRA-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].section_id` | `"SECTION-0155"` (`str`) | Configures `section id` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].page_number` | `120` (`int`) | Configures `page number` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].evidence_kind` | `"TECHNICAL_EQUIPMENT_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].exact_raw_excerpt` | `"installations nécessaires aux réseaux \nd’intérêt collectif, aux ouvrages publics d’infrastructures"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].excerpt_sha256` | `"886aaceafb2a40e73e3ebe145b3a58b6a22b239a8ce5fcc3740cb99d7d6298a0"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].section_page_fragment_sha256` | `"a23b2c0f7e48711758012d3b176967139a3b87d230c3dea312987693de86c369"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].excerpt_start` | `120` (`int`) | Configures `excerpt start` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].excerpt_end` | `218` (`int`) | Configures `excerpt end` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].interpretation_note` | `"This is an exact infrastructure exception; BESS qualification is unresolved."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].source_rule_id` | `"MURET-AUf0-ROUTE-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].source_rule_excerpt` | `"Sont interdites toutes les constructions autres que les installations nécessaires aux réseaux \nd’intérêt collectif, aux ouvrages publics d’infrastructures, et les extensions définies à \nl’article AUf0 – 2."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].source_rule_sha256` | `"5f4af2e2ece550ccc1bcb39eb7436f7dba04f5f21c0346b7d780bf350b77183c"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].source_rule_start` | `64` (`int`) | Configures `source rule start` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[0].source_rule_end` | `269` (`int`) | Configures `source rule end` under the exact parent path `chapters[10].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].evidence_id` | `"MURET-AUF0-MODIFICATION-CONDITION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].section_id` | `"SECTION-0156"` (`str`) | Configures `section id` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].page_number` | `120` (`int`) | Configures `page number` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].evidence_kind` | `"OTHER_RELEVANT_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].evidence_direction` | `"CONDITION"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].exact_raw_excerpt` | `"Les constructions et opérations nouvelles ne pourront être autorisées qu’après la \nmise en œuvre d’une procédure de modification du PLU"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].excerpt_sha256` | `"01594e632fb064a7e4dd408e68c156b66711abe6ae2a0e470c779e94b91f3a48"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].section_page_fragment_sha256` | `"15040ad2f1b4a5fd1c44bdfead25d12cf4ccdd53a5f28736e4c383ae19a6cac9"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].excerpt_start` | `100` (`int`) | Configures `excerpt start` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].excerpt_end` | `235` (`int`) | Configures `excerpt end` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].interpretation_note` | `"This prerequisite is a condition only and is not treated as route evidence."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].source_rule_id` | `"MURET-AUf0-CONDITION-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].source_rule_excerpt` | `"Les constructions et opérations nouvelles ne pourront être autorisées qu’après la \nmise en œuvre d’une procédure de modification du PLU."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].source_rule_sha256` | `"2d9633774f414a8ad2f8e42bfcbb2507b677906ca6aca480f0239cec007942e3"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].source_rule_start` | `100` (`int`) | Configures `source rule start` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].evidence[1].source_rule_end` | `236` (`int`) | Configures `source rule end` under the exact parent path `chapters[10].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].route_assessments[0].route_id` | `"MURET-AUf0-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[10].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].route_assessments[0].route_kind` | `"CONDITIONAL_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[10].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].route_assessments[0].positive_evidence_ids[0]` | `"MURET-AUF0-INFRA-ROUTE-01"` (`str`) | Ordered configured member of `chapters[10].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].route_assessments[0].condition_evidence_ids[0]` | `"MURET-AUF0-MODIFICATION-CONDITION-01"` (`str`) | Ordered configured member of `chapters[10].route_assessments[0].condition_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].route_assessments[0].difficulty_evidence_ids` | `[]` (`list`) | Configures `difficulty evidence ids` under the exact parent path `chapters[10].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[10].route_assessments[0].applicability_note` | `"The cited positive category and its explicit qualification are assessed as one coherent route; BESS applicability remains unresolved."` (`str`) | Configures `applicability note` under the exact parent path `chapters[10].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].resolved_zone_chapter_label` | `"A"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].reviewed_section_ids[0]` | `"SECTION-0170"` (`str`) | Ordered configured member of `chapters[11].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].reviewed_section_ids[1]` | `"SECTION-0171"` (`str`) | Ordered configured member of `chapters[11].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].review_note` | `"Articles A 1 and A 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].rationale` | `"Article A 1 contains broad restrictive language and a separate exception for necessary technical and infrastructure works. The policy records the conflict without deciding BESS qualification."` (`str`) | Configures `rationale` under the exact parent path `chapters[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].missing_information` | `"Formal necessity and BESS infrastructure classification, agricultural-zone effects, all Article A 1/2 provisions, prescriptions, servitudes and project design."` (`str`) | Configures `missing information` under the exact parent path `chapters[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].evidence_id` | `"MURET-A-RESTRICTION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].section_id` | `"SECTION-0170"` (`str`) | Configures `section id` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].page_number` | `125` (`int`) | Configures `page number` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].evidence_kind` | `"USE_RESTRICTION"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].evidence_direction` | `"SUPPORTS_DIFFICULTY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].exact_raw_excerpt` | `"Sont interdites toutes les occupations et utilisations du sol autres que celles"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].excerpt_sha256` | `"f18eba9dd56f410853fb685d30b6fcc78ee95359c6577387b78a29c3261b3c61"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].section_page_fragment_sha256` | `"51342e0ae335504d0f750e0138a63c2ffe928e11e564872122bec65edb4a8e13"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].excerpt_start` | `67` (`int`) | Configures `excerpt start` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].excerpt_end` | `146` (`int`) | Configures `excerpt end` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].interpretation_note` | `"This is the broad restriction phrase, separate from the exception."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].source_rule_id` | `"MURET-A-RESTRICTION-EXCEPTION-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].source_rule_excerpt` | `"Sont interdites toutes les occupations et utilisations du sol autres que celles : \n- nécessaires à l’exploitation agricole, qu’il s’agisse des constructions et extensions \nà usage d’habitation ou des constructions et installations à usage agricole, \n- nécessaires au bon fonctionnement des systèmes de gestion des eaux, \n- nécessaires aux ouvrages techniques et d’infrastructures, \n- mentionnées à l’article A2"` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].source_rule_sha256` | `"4a0a23edf39f707575293cb759d13f6bf2081db5df58ff1e9bed08c98775b1b9"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].source_rule_start` | `67` (`int`) | Configures `source rule start` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[0].source_rule_end` | `477` (`int`) | Configures `source rule end` under the exact parent path `chapters[11].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].evidence_id` | `"MURET-A-INFRA-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].section_id` | `"SECTION-0170"` (`str`) | Configures `section id` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].page_number` | `125` (`int`) | Configures `page number` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].evidence_kind` | `"TECHNICAL_EQUIPMENT_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].exact_raw_excerpt` | `"nécessaires aux ouvrages techniques et d’infrastructures"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].excerpt_sha256` | `"7eee9f7e595784b2d6a4b605a4f0b5703a0446acb77b7420c709b5516e30e0a2"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].section_page_fragment_sha256` | `"51342e0ae335504d0f750e0138a63c2ffe928e11e564872122bec65edb4a8e13"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].excerpt_start` | `390` (`int`) | Configures `excerpt start` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].excerpt_end` | `446` (`int`) | Configures `excerpt end` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].interpretation_note` | `"This is the separate technical-infrastructure exception; BESS necessity and classification are unresolved."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].source_rule_id` | `"MURET-A-RESTRICTION-EXCEPTION-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].source_rule_excerpt` | `"Sont interdites toutes les occupations et utilisations du sol autres que celles : \n- nécessaires à l’exploitation agricole, qu’il s’agisse des constructions et extensions \nà usage d’habitation ou des constructions et installations à usage agricole, \n- nécessaires au bon fonctionnement des systèmes de gestion des eaux, \n- nécessaires aux ouvrages techniques et d’infrastructures, \n- mentionnées à l’article A2"` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].source_rule_sha256` | `"4a0a23edf39f707575293cb759d13f6bf2081db5df58ff1e9bed08c98775b1b9"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].source_rule_start` | `67` (`int`) | Configures `source rule start` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].evidence[1].source_rule_end` | `477` (`int`) | Configures `source rule end` under the exact parent path `chapters[11].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].route_assessments[0].route_id` | `"MURET-A-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[11].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].route_assessments[0].route_kind` | `"RESTRICTION_EXCEPTION_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[11].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].route_assessments[0].positive_evidence_ids[0]` | `"MURET-A-INFRA-ROUTE-01"` (`str`) | Ordered configured member of `chapters[11].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].route_assessments[0].condition_evidence_ids` | `[]` (`list`) | Configures `condition evidence ids` under the exact parent path `chapters[11].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].route_assessments[0].difficulty_evidence_ids[0]` | `"MURET-A-RESTRICTION-01"` (`str`) | Ordered configured member of `chapters[11].route_assessments[0].difficulty_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[11].route_assessments[0].applicability_note` | `"The restriction and its listed exception are assessed as one coherent route; BESS applicability remains unresolved."` (`str`) | Configures `applicability note` under the exact parent path `chapters[11].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].resolved_zone_chapter_label` | `"N"` (`str`) | Configures `resolved zone chapter label` under the exact parent path `chapters[12]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].review_completeness` | `"COMPLETE_FOR_CONFIGURED_USE_CONTROL_ARTICLES"` (`str`) | Configures `review completeness` under the exact parent path `chapters[12]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].reviewed_section_ids[0]` | `"SECTION-0184"` (`str`) | Ordered configured member of `chapters[12].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].reviewed_section_ids[1]` | `"SECTION-0185"` (`str`) | Ordered configured member of `chapters[12].reviewed_section_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].review_note` | `"Articles N 1 and N 2 were reviewed in full for written use controls."` (`str`) | Configures `review note` under the exact parent path `chapters[12]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].zoning_precheck_status` | `"CONDITIONAL_REVIEW"` (`str`) | Configures `zoning precheck status` under the exact parent path `chapters[12]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].zoning_precheck_confidence` | `"LOW"` (`str`) | Configures `zoning precheck confidence` under the exact parent path `chapters[12]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].rationale` | `"Article N 1 contains a broad restriction and a separate exception for necessary technical and infrastructure equipment. The policy records the conflict without deciding BESS qualification."` (`str`) | Configures `rationale` under the exact parent path `chapters[12]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].missing_information` | `"Formal necessity and BESS infrastructure classification, natural-zone effects, all Article N 1/2 provisions, prescriptions, servitudes and project design."` (`str`) | Configures `missing information` under the exact parent path `chapters[12]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].evidence_id` | `"MURET-N-RESTRICTION-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].section_id` | `"SECTION-0184"` (`str`) | Configures `section id` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].page_number` | `135` (`int`) | Configures `page number` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].evidence_kind` | `"USE_RESTRICTION"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].evidence_direction` | `"SUPPORTS_DIFFICULTY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].exact_raw_excerpt` | `"Sont interdites, toutes les occupations et utilisations du sol, à l’exception"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].excerpt_sha256` | `"4781673bc1d5c704acd3be46c706805f6eaebacd4fc4b2296877af8bce6688ef"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].section_page_fragment_sha256` | `"0cac3a1aeb56859670b715c17e1c166959147a0f90a19a12f87e0025b263e195"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].excerpt_start` | `69` (`int`) | Configures `excerpt start` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].excerpt_end` | `146` (`int`) | Configures `excerpt end` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].interpretation_note` | `"This is the broad restriction phrase, separate from its listed exceptions."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].source_rule_id` | `"MURET-N-RESTRICTION-EXCEPTION-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].source_rule_excerpt` | `"Sont interdites, toutes les occupations et utilisations du sol, à l’exception : \n \n- des occupations et utilisations du sol soumises à des conditions particulières et \nrépertoriées à l’article N 2, \n- des équipements nécessaires aux ouvrages techniques et d’infrastructure, \n- des aménagements liés aux ouvrages techniques nécessaires au fonctionnement des \nservices publics, \n- des équipements nécessaires au bon fonctionnement des systèmes de gestion des \neaux, \n- en secteur NL : \n- les constructions, installations et utilisations du sol destinées à l’accueil des \nactivités de loisirs et d’équipements publics sportifs ou socio-culturels, \n- les terrains de camping et de caravaning, excepté dans le secteur inondable \nrepéré au plan de zonage."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].source_rule_sha256` | `"c434670531b43bbc23dd24c1fa01bba1eedaee0ba9e241b9f432255c74db30d2"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].source_rule_start` | `69` (`int`) | Configures `source rule start` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[0].source_rule_end` | `818` (`int`) | Configures `source rule end` under the exact parent path `chapters[12].evidence[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].evidence_id` | `"MURET-N-INFRA-ROUTE-01"` (`str`) | Configures `evidence id` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].section_id` | `"SECTION-0184"` (`str`) | Configures `section id` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].page_number` | `135` (`int`) | Configures `page number` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].evidence_kind` | `"TECHNICAL_EQUIPMENT_RULE"` (`str`) | Configures `evidence kind` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].evidence_direction` | `"SUPPORTS_POTENTIAL_COMPATIBILITY"` (`str`) | Configures `evidence direction` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].exact_raw_excerpt` | `"des équipements nécessaires aux ouvrages techniques et d’infrastructure"` (`str`) | Configures `exact raw excerpt` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].excerpt_sha256` | `"b28cb339936e8598faee5c5bba6f1be5f52e40b1ce6f33fe854ae1daff54d867"` (`str`) | Binds the exact bytes or canonical component named by `excerpt_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].section_page_fragment_sha256` | `"0cac3a1aeb56859670b715c17e1c166959147a0f90a19a12f87e0025b263e195"` (`str`) | Binds the exact bytes or canonical component named by `section_page_fragment_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].excerpt_start` | `270` (`int`) | Configures `excerpt start` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].excerpt_end` | `341` (`int`) | Configures `excerpt end` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].interpretation_note` | `"This is the separate technical-infrastructure exception; BESS necessity and classification are unresolved."` (`str`) | Configures `interpretation note` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].source_rule_id` | `"MURET-N-RESTRICTION-EXCEPTION-RULE-01"` (`str`) | Configures `source rule id` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].source_rule_excerpt` | `"Sont interdites, toutes les occupations et utilisations du sol, à l’exception : \n \n- des occupations et utilisations du sol soumises à des conditions particulières et \nrépertoriées à l’article N 2, \n- des équipements nécessaires aux ouvrages techniques et d’infrastructure, \n- des aménagements liés aux ouvrages techniques nécessaires au fonctionnement des \nservices publics, \n- des équipements nécessaires au bon fonctionnement des systèmes de gestion des \neaux, \n- en secteur NL : \n- les constructions, installations et utilisations du sol destinées à l’accueil des \nactivités de loisirs et d’équipements publics sportifs ou socio-culturels, \n- les terrains de camping et de caravaning, excepté dans le secteur inondable \nrepéré au plan de zonage."` (`str`) | Configures `source rule excerpt` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].source_rule_sha256` | `"c434670531b43bbc23dd24c1fa01bba1eedaee0ba9e241b9f432255c74db30d2"` (`str`) | Binds the exact bytes or canonical component named by `source_rule_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].source_rule_start` | `69` (`int`) | Configures `source rule start` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].evidence[1].source_rule_end` | `818` (`int`) | Configures `source rule end` under the exact parent path `chapters[12].evidence[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].route_assessments[0].route_id` | `"MURET-N-ROUTE-01"` (`str`) | Configures `route id` under the exact parent path `chapters[12].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].route_assessments[0].route_kind` | `"RESTRICTION_EXCEPTION_ROUTE"` (`str`) | Configures `route kind` under the exact parent path `chapters[12].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].route_assessments[0].positive_evidence_ids[0]` | `"MURET-N-INFRA-ROUTE-01"` (`str`) | Ordered configured member of `chapters[12].route_assessments[0].positive_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].route_assessments[0].condition_evidence_ids` | `[]` (`list`) | Configures `condition evidence ids` under the exact parent path `chapters[12].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].route_assessments[0].difficulty_evidence_ids[0]` | `"MURET-N-RESTRICTION-01"` (`str`) | Ordered configured member of `chapters[12].route_assessments[0].difficulty_evidence_ids`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `chapters[12].route_assessments[0].applicability_note` | `"The restriction and its listed exception are assessed as one coherent route; BESS applicability remains unresolved."` (`str`) | Configures `applicability note` under the exact parent path `chapters[12].route_assessments[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_interpret_bess_zoning.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |

## 5. Classes / models / dataclasses

Not applicable; this file declares no Python class.

## 6. Functions and methods

Not applicable; this file declares no Python function or method.

## 7. Data contracts

The exact byte-bound values or text lines above are the data contract for this file. Structured validators in consuming Python modules remain authoritative for types, nullability, allowed values, units, source provenance, calculations, and downstream semantics.

## 8. Interfaces

Direct literal-path consumers are listed above. Git, uv, Python, configuration loaders, documentation readers, or generated-data directory conventions consume project metadata according to the file type.

## 9. Error handling

This passive file raises no exception. Its consumers reject missing, malformed, unsupported, duplicate, semantically invalid, or stale content with their documented controlled errors.

## 10. Side effects

The file itself has no runtime side effect. A consumer may read it, resolve dependencies, configure tools, or use it as source/policy evidence; those effects belong to the consuming function.

## 11. Security / trust boundaries

The SHA256 binds this documentation to exact bytes. Checked-in configuration identity is necessary but does not replace physical source/hash verification performed by source-complete adapters.

## 12. GIS / CRS rules

Only structured CRS fields listed above impose a GIS rule, and their consuming validators define it. Otherwise not applicable.

## 13. Provenance rules

Checked-in source locks, URLs, hashes, versions, profile IDs, and evidence references are textual provenance inputs. Their consuming code determines whether and how physical bytes are revalidated.

## 14. Business meaning

This file supports the `planning` domain only through its exact checked-in values and current consumers.

## 15. Explicit non-goals

- Planning facts and prechecks do not constitute legal advice, authorization, or prohibition.

## 16. Tests

Tests that load or mention this path are documented in their companion files. No test is inferred solely from the filename.

## 17. Change impact

Changing these bytes requires reviewing every consuming validator, source/policy/config hash, generated result or artifact lineage, affected tests, and this companion SHA256. Dependency-lock changes also require `uv lock --check` and `uv pip check`.
