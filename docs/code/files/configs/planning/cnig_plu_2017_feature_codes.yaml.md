# `configs/planning/cnig_plu_2017_feature_codes.yaml`

## File identity

- Repository path: `configs/planning/cnig_plu_2017_feature_codes.yaml`
- File type: YAML configuration
- Primary responsibility: Defines the approved CNIG PLU v2017 official planning-feature code pairs, labels, references, and profile identity.
- Layer / domain: `checked-in configuration` / `planning`
- Public or internal role: Repository artifact; not a Python public API.
- Source SHA256: `77407429fd414eece8f6b20ca4da587aac76ab6b0b93e02f148622ab85ee253e`

## 1. Purpose

Defines the approved CNIG PLU v2017 official planning-feature code pairs, labels, references, and profile identity.

## 2. Position in LandScout architecture

This `checked-in configuration` artifact supplies exact checked-in bytes to the current repository. Consumers found by exact path reference are: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`.

## 3. Imports and dependencies

Not a Python module. Its consumers parse or interpret the bytes using the source/configuration functions identified by repository references and pipeline documentation.

## 4. Constants and domains

Every parsed leaf field is listed below; list indices preserve source order.

| Field path | Exact checked-in value | Contract role |
|---|---|---|
| `schema_version` | `2` (`int`) | Selects the strict configuration schema; unsupported versions are rejected. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `profile` | `"cnig_plu_2017_muret_observed_pairs_v2"` (`str`) | Configures `profile` under the exact parent path `<root>`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `standard_model` | `"CNIG PLU v2017"` (`str`) | Names the official planning standard/model against which records are validated. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `official_text_normalization` | `"GPU_DISPLAY_TEXT_NFC_WHITESPACE_V1"` (`str`) | Configures `official text normalization` under the exact parent path `<root>`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `official_sources.prescription` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/PrescriptionUrbaType"` (`str`) | Configures `prescription` under the exact parent path `official_sources`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `official_sources.information` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/InformationUrbaType"` (`str`) | Configures `information` under the exact parent path `official_sources`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `retrieval_date` | `"2026-08-12"` (`str`) | Configures `retrieval date` under the exact parent path `<root>`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `canonical_records_sha256` | `"5990552a681a9e50c072eb207bf88d25c876f61c89eeb88618e74d905487672c"` (`str`) | Binds the exact bytes or canonical component named by `canonical_records_sha256`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[0].feature_family` | `"INFORMATION"` (`str`) | Configures `feature family` under the exact parent path `records[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[0].type_code` | `"02"` (`str`) | Configures `type code` under the exact parent path `records[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[0].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `records[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[0].official_label` | `"Zone d'aménagement concerté"` (`str`) | Configures `official label` under the exact parent path `records[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[0].legal_reference` | `"L311-1 code de l’urbanisme"` (`str`) | Configures `legal reference` under the exact parent path `records[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[0].regulation_or_annex_reference` | `"R151-52 8°"` (`str`) | Configures `regulation or annex reference` under the exact parent path `records[0]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[0].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/InformationUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[1].feature_family` | `"INFORMATION"` (`str`) | Configures `feature family` under the exact parent path `records[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[1].type_code` | `"14"` (`str`) | Configures `type code` under the exact parent path `records[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[1].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `records[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[1].official_label` | `"Périmètre de voisinage d'infrastructure de transport terrestre (secteur affecté par le bruit)"` (`str`) | Configures `official label` under the exact parent path `records[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[1].legal_reference` | `"L571-10 code de l’environnement"` (`str`) | Configures `legal reference` under the exact parent path `records[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[1].regulation_or_annex_reference` | `"R151-53 5°"` (`str`) | Configures `regulation or annex reference` under the exact parent path `records[1]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[1].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/InformationUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[2].feature_family` | `"INFORMATION"` (`str`) | Configures `feature family` under the exact parent path `records[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[2].type_code` | `"27"` (`str`) | Configures `type code` under the exact parent path `records[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[2].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `records[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[2].official_label` | `"Plan d'exposition au bruit des aérodromes"` (`str`) | Configures `official label` under the exact parent path `records[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[2].legal_reference` | `"L112-6 code de l’urbanisme"` (`str`) | Configures `legal reference` under the exact parent path `records[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[2].regulation_or_annex_reference` | `"R151-52 2°"` (`str`) | Configures `regulation or annex reference` under the exact parent path `records[2]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[2].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/InformationUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[3].feature_family` | `"INFORMATION"` (`str`) | Configures `feature family` under the exact parent path `records[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[3].type_code` | `"99"` (`str`) | Configures `type code` under the exact parent path `records[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[3].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `records[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[3].official_label` | `"Autre périmètre, secteur, plan, document, site, projet, espace."` (`str`) | Configures `official label` under the exact parent path `records[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[3].legal_reference` | `null` (`NoneType`) | Configures `legal reference` under the exact parent path `records[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[3].regulation_or_annex_reference` | `null` (`NoneType`) | Configures `regulation or annex reference` under the exact parent path `records[3]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[3].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/InformationUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[4].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `records[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[4].type_code` | `"01"` (`str`) | Configures `type code` under the exact parent path `records[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[4].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `records[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[4].official_label` | `"Espace boisé classé"` (`str`) | Configures `official label` under the exact parent path `records[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[4].legal_reference` | `"L113-1"` (`str`) | Configures `legal reference` under the exact parent path `records[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[4].regulation_or_annex_reference` | `"R151-31 1°"` (`str`) | Configures `regulation or annex reference` under the exact parent path `records[4]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[4].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/PrescriptionUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[5].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `records[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[5].type_code` | `"05"` (`str`) | Configures `type code` under the exact parent path `records[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[5].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `records[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[5].official_label` | `"Emplacement réservé"` (`str`) | Configures `official label` under the exact parent path `records[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[5].legal_reference` | `"L151-41 1° à 3°"` (`str`) | Configures `legal reference` under the exact parent path `records[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[5].regulation_or_annex_reference` | `"R151-34 4°, R151-38 1°, R151-43 3°, R151-48 2°, R151-50 1°"` (`str`) | Configures `regulation or annex reference` under the exact parent path `records[5]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[5].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/PrescriptionUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[6].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `records[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[6].type_code` | `"07"` (`str`) | Configures `type code` under the exact parent path `records[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[6].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `records[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[6].official_label` | `"Patrimoine bâti, paysager ou éléments de paysages à protéger pour des motifs d'ordre culturel, historique, architectural ou écologique"` (`str`) | Configures `official label` under the exact parent path `records[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[6].legal_reference` | `"L151-19 et L151-23"` (`str`) | Configures `legal reference` under the exact parent path `records[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[6].regulation_or_annex_reference` | `"R151-41 3° Et R151-43"` (`str`) | Configures `regulation or annex reference` under the exact parent path `records[6]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[6].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/PrescriptionUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[7].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `records[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[7].type_code` | `"07"` (`str`) | Configures `type code` under the exact parent path `records[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[7].subtype_code` | `"04"` (`str`) | Configures `subtype code` under the exact parent path `records[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[7].official_label` | `"Éléments de paysage, (sites et secteurs) à préserver pour des motifs d'ordre écologique"` (`str`) | Configures `official label` under the exact parent path `records[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[7].legal_reference` | `"L151-23"` (`str`) | Configures `legal reference` under the exact parent path `records[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[7].regulation_or_annex_reference` | `"R151-43 5°"` (`str`) | Configures `regulation or annex reference` under the exact parent path `records[7]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[7].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/PrescriptionUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[8].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `records[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[8].type_code` | `"15"` (`str`) | Configures `type code` under the exact parent path `records[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[8].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `records[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[8].official_label` | `"Règles d’implantation des constructions"` (`str`) | Configures `official label` under the exact parent path `records[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[8].legal_reference` | `"L151-17 et L151-18"` (`str`) | Configures `legal reference` under the exact parent path `records[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[8].regulation_or_annex_reference` | `"R151-39 dernier al."` (`str`) | Configures `regulation or annex reference` under the exact parent path `records[8]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[8].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/PrescriptionUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[9].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `records[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[9].type_code` | `"15"` (`str`) | Configures `type code` under the exact parent path `records[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[9].subtype_code` | `"01"` (`str`) | Configures `subtype code` under the exact parent path `records[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[9].official_label` | `"Implantation des constructions par rapport aux voies et aux emprises publiques"` (`str`) | Configures `official label` under the exact parent path `records[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[9].legal_reference` | `"L151-17 et L151-18"` (`str`) | Configures `legal reference` under the exact parent path `records[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[9].regulation_or_annex_reference` | `"R151-39"` (`str`) | Configures `regulation or annex reference` under the exact parent path `records[9]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[9].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/PrescriptionUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[10].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `records[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[10].type_code` | `"17"` (`str`) | Configures `type code` under the exact parent path `records[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[10].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `records[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[10].official_label` | `"Secteur à programme de logements mixité sociale en zone U et AU"` (`str`) | Configures `official label` under the exact parent path `records[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[10].legal_reference` | `"L151-15"` (`str`) | Configures `legal reference` under the exact parent path `records[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[10].regulation_or_annex_reference` | `"R151-38 3°"` (`str`) | Configures `regulation or annex reference` under the exact parent path `records[10]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[10].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/PrescriptionUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[11].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `records[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[11].type_code` | `"18"` (`str`) | Configures `type code` under the exact parent path `records[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[11].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `records[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[11].official_label` | `"Périmètre comportant des orientations d’aménagement et de programmation (OAP)"` (`str`) | Configures `official label` under the exact parent path `records[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[11].legal_reference` | `"L151-6 et L151-7"` (`str`) | Configures `legal reference` under the exact parent path `records[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[11].regulation_or_annex_reference` | `"R151-6 à R151-8-1"` (`str`) | Configures `regulation or annex reference` under the exact parent path `records[11]`. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `records[11].official_source_url` | `"https://www.geoportail-urbanisme.gouv.fr/standard/cnig_PLU_2017/codes/PrescriptionUrbaType"` (`str`) | Configures the exact official source url; HTTPS/origin/path validation is defined by the consuming model. Consumers found by exact repository path reference: `docs/DEV_LOG.md`, `tests/unit/test_bess_planning_feature_policy.py`, `tests/unit/test_resolve_planning_feature_codes.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |

## 5. Classes / models / dataclasses

Not applicable; this file declares no Python class.

## 6. Functions and methods

Not applicable; this file declares no Python function or method.

## 7. Data contracts

The exact byte-bound values or text lines above are the data contract for this file. Structured validators in consuming Python modules remain authoritative for types, nullability, allowed values, units, source provenance, calculations, and downstream semantics.

## 8. Interfaces

Direct literal-path consumers are listed above. Git, uv, Python, configuration loaders, documentation readers, or generated-data directory conventions consume project metadata according to the file type.

## 9. Error handling

This passive file raises no exception. Its consumers reject missing, malformed, unsupported, duplicate, semantically invalid, or stale content with their documented controlled errors.

## 10. Side effects

The file itself has no runtime side effect. A consumer may read it, resolve dependencies, configure tools, or use it as source/policy evidence; those effects belong to the consuming function.

## 11. Security / trust boundaries

The SHA256 binds this documentation to exact bytes. Checked-in configuration identity is necessary but does not replace physical source/hash verification performed by source-complete adapters.

## 12. GIS / CRS rules

Only structured CRS fields listed above impose a GIS rule, and their consuming validators define it. Otherwise not applicable.

## 13. Provenance rules

Checked-in source locks, URLs, hashes, versions, profile IDs, and evidence references are textual provenance inputs. Their consuming code determines whether and how physical bytes are revalidated.

## 14. Business meaning

This file supports the `planning` domain only through its exact checked-in values and current consumers.

## 15. Explicit non-goals

- Planning facts and prechecks do not constitute legal advice, authorization, or prohibition.

## 16. Tests

Tests that load or mention this path are documented in their companion files. No test is inferred solely from the filename.

## 17. Change impact

Changing these bytes requires reviewing every consuming validator, source/policy/config hash, generated result or artifact lineage, affected tests, and this companion SHA256. Dependency-lock changes also require `uv lock --check` and `uv pip check`.
