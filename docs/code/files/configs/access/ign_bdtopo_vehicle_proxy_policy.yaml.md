# `configs/access/ign_bdtopo_vehicle_proxy_policy.yaml`

## File identity

- Repository path: `configs/access/ign_bdtopo_vehicle_proxy_policy.yaml`
- File type: YAML configuration
- Primary responsibility: Defines the approved versioned IGN general-car/light-vehicle evidence policy, source references, vocabularies, outcomes, and exact precedence.
- Layer / domain: `checked-in configuration` / `road`
- Public or internal role: Repository artifact; not a Python public API.
- Source SHA256: `2092bc620063ec1176b2abebaefafcc108a42793992dd18f869d44fdb07ca166`

## 1. Purpose

Defines the approved versioned IGN general-car/light-vehicle evidence policy, source references, vocabularies, outcomes, and exact precedence.

## 2. Position in LandScout architecture

This `checked-in configuration` artifact supplies exact checked-in bytes to the current repository. Consumers found by exact path reference are: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`.

## 3. Imports and dependencies

Not a Python module. Its consumers parse or interpret the bytes using the source/configuration functions identified by repository references and pipeline documentation.

## 4. Constants and domains

Every parsed leaf field is listed below; list indices preserve source order.

| Field path | Exact checked-in value | Contract role |
|---|---|---|
| `policy_id` | `"ign_bdtopo_general_vehicle_proxy_v2"` (`str`) | Identifies the compiled policy lineage propagated to result rows. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `schema_version` | `2` (`int`) | Selects the strict configuration schema; unsupported versions are rejected. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `scope` | `"OFFICIAL_IGN_CAR_ROUTING_EVIDENCE_ONLY"` (`str`) | Configures `scope` under the exact parent path `<root>`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `references.navigation.publisher` | `"IGN"` (`str`) | Configures `publisher` under the exact parent path `references.navigation`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `references.navigation.title` | `"Calcul d’itinéraire"` (`str`) | Configures `title` under the exact parent path `references.navigation`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `references.navigation.revision` | `"2026-05-27"` (`str`) | Configures `revision` under the exact parent path `references.navigation`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `references.navigation.evidence_scope` | `"GENERAL_CAR_ROUTING_RULES"` (`str`) | Configures `evidence scope` under the exact parent path `references.navigation`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `references.bdtopo_product.publisher` | `"IGN"` (`str`) | Configures `publisher` under the exact parent path `references.bdtopo_product`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `references.bdtopo_product.title` | `"BD TOPO® Version 3.5 - Descriptif de contenu"` (`str`) | Configures `title` under the exact parent path `references.bdtopo_product`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `references.bdtopo_product.document_id` | `"DC_BDTOPO_3-5"` (`str`) | Configures `document id` under the exact parent path `references.bdtopo_product`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `references.bdtopo_product.revision` | `"2025-11"` (`str`) | Configures `revision` under the exact parent path `references.bdtopo_product`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `references.bdtopo_product.evidence_scope` | `"SOURCE_ATTRIBUTE_SEMANTICS"` (`str`) | Configures `evidence scope` under the exact parent path `references.bdtopo_product`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `evidence_checked_on` | `"2026-08-16"` (`str`) | Configures `evidence checked on` under the exact parent path `<root>`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `vehicle_scope` | `"LIGHT_VEHICLE_AND_GENERAL_CAR_NETWORK"` (`str`) | Configures `vehicle scope` under the exact parent path `<root>`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `heavy_vehicle_access` | `"NOT_PROVEN"` (`str`) | Configures `heavy vehicle access` under the exact parent path `<root>`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `classes.general_vehicle_proxy` | `"GENERAL_VEHICLE_PROXY"` (`str`) | Configures `general vehicle proxy` under the exact parent path `classes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `classes.limited_vehicle_proxy` | `"LIMITED_VEHICLE_PROXY"` (`str`) | Configures `limited vehicle proxy` under the exact parent path `classes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `classes.restricted_review` | `"RESTRICTED_REVIEW"` (`str`) | Configures `restricted review` under the exact parent path `classes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `classes.not_general_vehicle_proxy` | `"NOT_GENERAL_VEHICLE_PROXY"` (`str`) | Configures `not general vehicle proxy` under the exact parent path `classes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `classes.not_distance_proxy` | `"NOT_DISTANCE_PROXY"` (`str`) | Configures `not distance proxy` under the exact parent path `classes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `classes.unknown_review` | `"UNKNOWN_REVIEW"` (`str`) | Configures `unknown review` under the exact parent path `classes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.asset_state.in_service[0]` | `"En service"` (`str`) | Ordered configured member of `source_values.asset_state.in_service`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.asset_state.project_geometry_not_significant[0]` | `"En projet"` (`str`) | Ordered configured member of `source_values.asset_state.project_geometry_not_significant`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.asset_state.under_construction[0]` | `"En construction"` (`str`) | Ordered configured member of `source_values.asset_state.under_construction`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.light_vehicle_access.open[0]` | `"Libre"` (`str`) | Ordered configured member of `source_values.light_vehicle_access.open`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.light_vehicle_access.toll[0]` | `"A péage"` (`str`) | Ordered configured member of `source_values.light_vehicle_access.toll`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.light_vehicle_access.rights_restricted[0]` | `"Restreint aux ayants droit"` (`str`) | Ordered configured member of `source_values.light_vehicle_access.rights_restricted`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.light_vehicle_access.physically_impossible[0]` | `"Physiquement impossible"` (`str`) | Ordered configured member of `source_values.light_vehicle_access.physically_impossible`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.nature.general_motor_road[0]` | `"Route à 1 chaussée"` (`str`) | Ordered configured member of `source_values.nature.general_motor_road`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.nature.general_motor_road[1]` | `"Route à 2 chaussées"` (`str`) | Ordered configured member of `source_values.nature.general_motor_road`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.nature.general_motor_road[2]` | `"Rond-point"` (`str`) | Ordered configured member of `source_values.nature.general_motor_road`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.nature.general_motor_road[3]` | `"Bretelle"` (`str`) | Ordered configured member of `source_values.nature.general_motor_road`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.nature.general_motor_road[4]` | `"Type autoroutier"` (`str`) | Ordered configured member of `source_values.nature.general_motor_road`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.nature.limited_motor_proxy[0]` | `"Route empierrée"` (`str`) | Ordered configured member of `source_values.nature.limited_motor_proxy`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.nature.limited_motor_proxy[1]` | `"Chemin"` (`str`) | Ordered configured member of `source_values.nature.limited_motor_proxy`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.nature.non_general_vehicle[0]` | `"Escalier"` (`str`) | Ordered configured member of `source_values.nature.non_general_vehicle`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.nature.non_general_vehicle[1]` | `"Sentier"` (`str`) | Ordered configured member of `source_values.nature.non_general_vehicle`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.nature.non_general_vehicle[2]` | `"Piste cyclable"` (`str`) | Ordered configured member of `source_values.nature.non_general_vehicle`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.nature.special_review[0]` | `"Bac ou liaison maritime"` (`str`) | Ordered configured member of `source_values.nature.special_review`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.known_restriction_review[0]` | `"Plot amovible"` (`str`) | Ordered configured member of `source_values.known_restriction_review`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.known_restriction_review[1]` | `"Voie de tramway utilisable par les véhicules de secours"` (`str`) | Ordered configured member of `source_values.known_restriction_review`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.known_restriction_review[2]` | `"Voie verte"` (`str`) | Ordered configured member of `source_values.known_restriction_review`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.known_restriction_review[3]` | `"Aménagement mixte hors voie verte"` (`str`) | Ordered configured member of `source_values.known_restriction_review`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.known_restriction_review[4]` | `"Piste cyclable"` (`str`) | Ordered configured member of `source_values.known_restriction_review`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.known_restriction_review[5]` | `"Entrée avec gardien"` (`str`) | Ordered configured member of `source_values.known_restriction_review`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.known_restriction_review[6]` | `"Passage barré"` (`str`) | Ordered configured member of `source_values.known_restriction_review`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.importance.known[0]` | `"1"` (`str`) | Ordered configured member of `source_values.importance.known`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.importance.known[1]` | `"2"` (`str`) | Ordered configured member of `source_values.importance.known`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.importance.known[2]` | `"3"` (`str`) | Ordered configured member of `source_values.importance.known`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.importance.known[3]` | `"4"` (`str`) | Ordered configured member of `source_values.importance.known`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.importance.known[4]` | `"5"` (`str`) | Ordered configured member of `source_values.importance.known`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.importance.known[5]` | `"6"` (`str`) | Ordered configured member of `source_values.importance.known`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.importance.limited[0]` | `"6"` (`str`) | Ordered configured member of `source_values.importance.limited`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_values.width_below_m` | `2.9` (`float`) | Configures width below m in metres. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[0]` | `"FICTITIOUS_GEOMETRY"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[1]` | `"PROJECT_GEOMETRY_NOT_SIGNIFICANT"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[2]` | `"NOT_IN_SERVICE"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[3]` | `"PHYSICALLY_IMPOSSIBLE"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[4]` | `"NON_GENERAL_VEHICLE_NATURE"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[5]` | `"RIGHTS_RESTRICTED"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[6]` | `"PRIVATE_ROAD"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[7]` | `"TEMPORAL_CLOSURE"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[8]` | `"KNOWN_RESTRICTION"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[9]` | `"OTHER_RECORDED_RESTRICTION"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[10]` | `"SPECIAL_NATURE"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[11]` | `"LIMITED_NATURE"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[12]` | `"IMPORTANCE_6"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[13]` | `"NARROW_CARRIAGEWAY"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[14]` | `"OPEN_OR_TOLL"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_precedence[15]` | `"UNKNOWN"` (`str`) | Ordered configured member of `decision_precedence`; order and uniqueness are validated/consumed where required. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.fictitious_geometry` | `"NOT_DISTANCE_PROXY"` (`str`) | Configures `fictitious geometry` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.project_geometry_not_significant` | `"NOT_DISTANCE_PROXY"` (`str`) | Configures `project geometry not significant` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.not_in_service` | `"NOT_GENERAL_VEHICLE_PROXY"` (`str`) | Configures `not in service` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.physically_impossible` | `"NOT_GENERAL_VEHICLE_PROXY"` (`str`) | Configures `physically impossible` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.non_general_vehicle_nature` | `"NOT_GENERAL_VEHICLE_PROXY"` (`str`) | Configures `non general vehicle nature` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.rights_restricted` | `"RESTRICTED_REVIEW"` (`str`) | Configures `rights restricted` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.private_road` | `"RESTRICTED_REVIEW"` (`str`) | Configures `private road` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.temporal_closure` | `"RESTRICTED_REVIEW"` (`str`) | Configures `temporal closure` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.known_restriction` | `"RESTRICTED_REVIEW"` (`str`) | Configures `known restriction` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.other_recorded_restriction` | `"RESTRICTED_REVIEW"` (`str`) | Configures `other recorded restriction` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.special_nature` | `"RESTRICTED_REVIEW"` (`str`) | Configures `special nature` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.limited_nature` | `"LIMITED_VEHICLE_PROXY"` (`str`) | Configures `limited nature` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.importance_6` | `"LIMITED_VEHICLE_PROXY"` (`str`) | Configures `importance 6` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.narrow_carriageway` | `"LIMITED_VEHICLE_PROXY"` (`str`) | Configures `narrow carriageway` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.open_or_toll` | `"GENERAL_VEHICLE_PROXY"` (`str`) | Configures `open or toll` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `decision_outcomes.unknown` | `"UNKNOWN_REVIEW"` (`str`) | Configures `unknown` under the exact parent path `decision_outcomes`. Consumers found by exact repository path reference: `tests/unit/test_apply_road_vehicle_proxy_policy.py`, `tests/unit/test_assess_road_proximity_coverage.py`, `tests/unit/test_enrich_road_proximity.py`, `tests/unit/test_road_vehicle_proxy_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |

## 5. Classes / models / dataclasses

Not applicable; this file declares no Python class.

## 6. Functions and methods

Not applicable; this file declares no Python function or method.

## 7. Data contracts

The exact byte-bound values or text lines above are the data contract for this file. Structured validators in consuming Python modules remain authoritative for types, nullability, allowed values, units, source provenance, calculations, and downstream semantics.

## 8. Interfaces

Direct literal-path consumers are listed above. Git, uv, Python, configuration loaders, documentation readers, or generated-data directory conventions consume project metadata according to the file type.

## 9. Error handling

This passive file raises no exception. Its consumers reject missing, malformed, unsupported, duplicate, semantically invalid, or stale content with their documented controlled errors.

## 10. Side effects

The file itself has no runtime side effect. A consumer may read it, resolve dependencies, configure tools, or use it as source/policy evidence; those effects belong to the consuming function.

## 11. Security / trust boundaries

The SHA256 binds this documentation to exact bytes. Checked-in configuration identity is necessary but does not replace physical source/hash verification performed by source-complete adapters.

## 12. GIS / CRS rules

Only structured CRS fields listed above impose a GIS rule, and their consuming validators define it. Otherwise not applicable.

## 13. Provenance rules

Checked-in source locks, URLs, hashes, versions, profile IDs, and evidence references are textual provenance inputs. Their consuming code determines whether and how physical bytes are revalidated.

## 14. Business meaning

This file supports the `road` domain only through its exact checked-in values and current consumers.

## 15. Explicit non-goals

- Road geometry and general-car evidence do not prove legal parcel access or heavy/construction-vehicle access.

## 16. Tests

Tests that load or mention this path are documented in their companion files. No test is inferred solely from the filename.

## 17. Change impact

Changing these bytes requires reviewing every consuming validator, source/policy/config hash, generated result or artifact lineage, affected tests, and this companion SHA256. Dependency-lock changes also require `uv lock --check` and `uv pip check`.
