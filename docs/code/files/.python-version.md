# `.python-version`

## File identity

- Repository path: `.python-version`
- Responsibility: Interpreter selection.
- Source SHA256: `7b55f8e67b5623c4bef3fa691288da9437d79d3aba156de48d481db32ac7d16d`
- Source SHA256 basis: `git-content`

The literal `3.12` selects the Python **minor series**, not a specific patch release. `pyproject.toml` independently requires `>=3.12,<3.13`; `uv.lock` resolves `==3.12.*`. The audit's observed Python 3.12.13 runtime is execution evidence, not a patch pin hidden in this file.

uv or a compatible version manager consumes this passive selection file. It executes no LandScout code. A future version change must review the project constraint, lock/native dependencies and full validation; documentation alone must not rewrite the pin.

## Exact complete current file content

The fenced UTF-8 content below equals the Git-stored file bytes.

````text
3.12
````
