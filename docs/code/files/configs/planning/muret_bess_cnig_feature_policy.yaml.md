# `configs/planning/muret_bess_cnig_feature_policy.yaml`

## File identity

- Repository path: `configs/planning/muret_bess_cnig_feature_policy.yaml`
- File type: YAML configuration
- Primary responsibility: Defines the Muret BESS precheck policy over official CNIG feature-code meaning only.
- Layer / domain: `checked-in configuration` / `planning`
- Public or internal role: Repository artifact; not a Python public API.
- Source SHA256: `8a26fcb8ee7e2f028baca65d94c2b5be5445ba9fc4b82b9b1a21843f933f8b2a`

## 1. Purpose

Defines the Muret BESS precheck policy over official CNIG feature-code meaning only.

## 2. Position in LandScout architecture

This `checked-in configuration` artifact supplies exact checked-in bytes to the current repository. Consumers found by exact path reference are: `tests/unit/test_bess_planning_feature_policy.py`.

## 3. Imports and dependencies

Not a Python module. Its consumers parse or interpret the bytes using the source/configuration functions identified by repository references and pipeline documentation.

## 4. Constants and domains

Every parsed leaf field is listed below; list indices preserve source order.

| Field path | Exact checked-in value | Contract role |
|---|---|---|
| `schema_version` | `1` (`int`) | Selects the strict configuration schema; unsupported versions are rejected. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `profile` | `"muret_bess_cnig_feature_policy_v1"` (`str`) | Configures `profile` under the exact parent path `<root>`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `policy_scope` | `"OFFICIAL_CNIG_CODE_MEANING_ONLY"` (`str`) | Limits the kind of source evidence the policy is allowed to interpret. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `local_feature_text_interpreted` | `false` (`bool`) | Enables/disables the exact local feature text interpreted behavior; Boolean coercion rules belong to the consuming model. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `local_regulation_content_interpreted` | `false` (`bool`) | Enables/disables the exact local regulation content interpreted behavior; Boolean coercion rules belong to the consuming model. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `legal_conclusion_produced` | `false` (`bool`) | Enables/disables the exact legal conclusion produced behavior; Boolean coercion rules belong to the consuming model. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.document_id` | `"33edb4c9f6943c88d8d92518bff20bec"` (`str`) | Configures `document id` under the exact parent path `source_lock`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.archive_sha256` | `"9d6677cd6634b56b712311042f0cc714d5ca42a38f82a417b27dd473255d7d93"` (`str`) | Binds the exact bytes or canonical component named by `archive_sha256`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.cnig_profile` | `"cnig_plu_2017_muret_observed_pairs_v2"` (`str`) | Configures `cnig profile` under the exact parent path `source_lock`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.cnig_profile_schema_version` | `2` (`int`) | Selects compatibility for the cnig profile schema version; the consuming model accepts only supported integers. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.cnig_profile_sha256` | `"5611b814eb4bc057578b908c6505094f9df5d2c2bf4ca126629b1362983c47ee"` (`str`) | Binds the exact bytes or canonical component named by `cnig_profile_sha256`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.cnig_result_hash_schema_version` | `5` (`int`) | Selects compatibility for the cnig result hash schema version; the consuming model accepts only supported integers. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `source_lock.cnig_complete_result_content_sha256` | `"b56b195b32914583e6599fe96b3d29977c52450c9755228d89ce7e192903ab3e"` (`str`) | Binds the exact bytes or canonical component named by `cnig_complete_result_content_sha256`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `status_priority.LIKELY_MATERIAL_CONSTRAINT` | `50` (`int`) | Configures `LIKELY MATERIAL CONSTRAINT` under the exact parent path `status_priority`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `status_priority.UNKNOWN` | `40` (`int`) | Configures `UNKNOWN` under the exact parent path `status_priority`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `status_priority.MATERIAL_REVIEW_REQUIRED` | `30` (`int`) | Configures `MATERIAL REVIEW REQUIRED` under the exact parent path `status_priority`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `status_priority.DESIGN_REVIEW_REQUIRED` | `20` (`int`) | Configures `DESIGN REVIEW REQUIRED` under the exact parent path `status_priority`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `status_priority.CONTEXT_REVIEW_REQUIRED` | `10` (`int`) | Configures `CONTEXT REVIEW REQUIRED` under the exact parent path `status_priority`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `canonical_policy_entries_sha256` | `"1d3e63f1123000402065b74402cb1e2295db2ac5655209ce410aaf36bfc2be91"` (`str`) | Binds the exact bytes or canonical component named by `canonical_policy_entries_sha256`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[0].feature_family` | `"INFORMATION"` (`str`) | Configures `feature family` under the exact parent path `entries[0]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[0].type_code` | `"02"` (`str`) | Configures `type code` under the exact parent path `entries[0]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[0].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `entries[0]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[0].expected_official_label` | `"Zone d'aménagement concerté"` (`str`) | Configures `expected official label` under the exact parent path `entries[0]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[0].expected_legal_reference` | `"L311-1 code de l’urbanisme"` (`str`) | Configures `expected legal reference` under the exact parent path `entries[0]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[0].expected_regulation_reference` | `"R151-52 8°"` (`str`) | Configures `expected regulation reference` under the exact parent path `entries[0]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[0].precheck_status` | `"CONTEXT_REVIEW_REQUIRED"` (`str`) | Configures `precheck status` under the exact parent path `entries[0]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[0].confidence` | `"HIGH"` (`str`) | Configures `confidence` under the exact parent path `entries[0]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[0].rationale` | `"The official code identifies a concerted-development-zone context that requires planning-document review but does not establish a direct BESS constraint by itself."` (`str`) | Configures `rationale` under the exact parent path `entries[0]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[0].required_human_action` | `"Review the applicable planning documents and authority context for the identified concerted-development zone."` (`str`) | Configures `required human action` under the exact parent path `entries[0]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[0].limitations` | `"This classification uses only the official CNIG code meaning and does not interpret any local feature text or regulation."` (`str`) | Configures `limitations` under the exact parent path `entries[0]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[1].feature_family` | `"INFORMATION"` (`str`) | Configures `feature family` under the exact parent path `entries[1]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[1].type_code` | `"14"` (`str`) | Configures `type code` under the exact parent path `entries[1]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[1].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `entries[1]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[1].expected_official_label` | `"Périmètre de voisinage d'infrastructure de transport terrestre (secteur affecté par le bruit)"` (`str`) | Configures `expected official label` under the exact parent path `entries[1]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[1].expected_legal_reference` | `"L571-10 code de l’environnement"` (`str`) | Configures `expected legal reference` under the exact parent path `entries[1]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[1].expected_regulation_reference` | `"R151-53 5°"` (`str`) | Configures `expected regulation reference` under the exact parent path `entries[1]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[1].precheck_status` | `"CONTEXT_REVIEW_REQUIRED"` (`str`) | Configures `precheck status` under the exact parent path `entries[1]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[1].confidence` | `"HIGH"` (`str`) | Configures `confidence` under the exact parent path `entries[1]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[1].rationale` | `"The official code identifies transport-infrastructure noise context that must be checked but does not establish a direct BESS constraint by itself."` (`str`) | Configures `rationale` under the exact parent path `entries[1]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[1].required_human_action` | `"Review the applicable noise-sector documents and project-specific context with the competent authority."` (`str`) | Configures `required human action` under the exact parent path `entries[1]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[1].limitations` | `"This classification uses only the official CNIG code meaning and does not interpret local noise rules or project effects."` (`str`) | Configures `limitations` under the exact parent path `entries[1]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[2].feature_family` | `"INFORMATION"` (`str`) | Configures `feature family` under the exact parent path `entries[2]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[2].type_code` | `"27"` (`str`) | Configures `type code` under the exact parent path `entries[2]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[2].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `entries[2]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[2].expected_official_label` | `"Plan d'exposition au bruit des aérodromes"` (`str`) | Configures `expected official label` under the exact parent path `entries[2]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[2].expected_legal_reference` | `"L112-6 code de l’urbanisme"` (`str`) | Configures `expected legal reference` under the exact parent path `entries[2]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[2].expected_regulation_reference` | `"R151-52 2°"` (`str`) | Configures `expected regulation reference` under the exact parent path `entries[2]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[2].precheck_status` | `"CONTEXT_REVIEW_REQUIRED"` (`str`) | Configures `precheck status` under the exact parent path `entries[2]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[2].confidence` | `"HIGH"` (`str`) | Configures `confidence` under the exact parent path `entries[2]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[2].rationale` | `"The official code identifies an aerodrome noise-exposure-plan context that must be checked but does not establish a direct BESS constraint by itself."` (`str`) | Configures `rationale` under the exact parent path `entries[2]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[2].required_human_action` | `"Review the applicable aerodrome noise-exposure plan and project-specific context with the competent authority."` (`str`) | Configures `required human action` under the exact parent path `entries[2]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[2].limitations` | `"This classification uses only the official CNIG code meaning and does not interpret the local plan or determine project admissibility."` (`str`) | Configures `limitations` under the exact parent path `entries[2]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[3].feature_family` | `"INFORMATION"` (`str`) | Configures `feature family` under the exact parent path `entries[3]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[3].type_code` | `"99"` (`str`) | Configures `type code` under the exact parent path `entries[3]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[3].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `entries[3]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[3].expected_official_label` | `"Autre périmètre, secteur, plan, document, site, projet, espace."` (`str`) | Configures `expected official label` under the exact parent path `entries[3]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[3].expected_legal_reference` | `null` (`NoneType`) | Configures `expected legal reference` under the exact parent path `entries[3]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[3].expected_regulation_reference` | `null` (`NoneType`) | Configures `expected regulation reference` under the exact parent path `entries[3]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[3].precheck_status` | `"UNKNOWN"` (`str`) | Configures `precheck status` under the exact parent path `entries[3]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[3].confidence` | `"LOW"` (`str`) | Configures `confidence` under the exact parent path `entries[3]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[3].rationale` | `"The official code is an unspecified other-information category and is too generic for a more precise BESS precheck."` (`str`) | Configures `rationale` under the exact parent path `entries[3]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[3].required_human_action` | `"Identify and review the feature-specific local source before drawing any planning inference."` (`str`) | Configures `required human action` under the exact parent path `entries[3]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[3].limitations` | `"The official code alone does not identify the local subject, rule, effect, authorization, or prohibition."` (`str`) | Configures `limitations` under the exact parent path `entries[3]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[4].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `entries[4]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[4].type_code` | `"01"` (`str`) | Configures `type code` under the exact parent path `entries[4]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[4].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `entries[4]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[4].expected_official_label` | `"Espace boisé classé"` (`str`) | Configures `expected official label` under the exact parent path `entries[4]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[4].expected_legal_reference` | `"L113-1"` (`str`) | Configures `expected legal reference` under the exact parent path `entries[4]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[4].expected_regulation_reference` | `"R151-31 1°"` (`str`) | Configures `expected regulation reference` under the exact parent path `entries[4]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[4].precheck_status` | `"LIKELY_MATERIAL_CONSTRAINT"` (`str`) | Configures `precheck status` under the exact parent path `entries[4]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[4].confidence` | `"HIGH"` (`str`) | Configures `confidence` under the exact parent path `entries[4]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[4].rationale` | `"The official code identifies a classified wooded-area protection family likely to be material for a BESS project without meaning prohibited."` (`str`) | Configures `rationale` under the exact parent path `entries[4]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[4].required_human_action` | `"Review the exact classified-area geometry, local prescription, applicable planning provisions, and project design with the competent authority."` (`str`) | Configures `required human action` under the exact parent path `entries[4]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[4].limitations` | `"This preliminary classification does not interpret the local prescription or establish authorization or prohibition."` (`str`) | Configures `limitations` under the exact parent path `entries[4]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[5].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `entries[5]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[5].type_code` | `"05"` (`str`) | Configures `type code` under the exact parent path `entries[5]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[5].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `entries[5]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[5].expected_official_label` | `"Emplacement réservé"` (`str`) | Configures `expected official label` under the exact parent path `entries[5]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[5].expected_legal_reference` | `"L151-41 1° à 3°"` (`str`) | Configures `expected legal reference` under the exact parent path `entries[5]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[5].expected_regulation_reference` | `"R151-34 4°, R151-38 1°, R151-43 3°, R151-48 2°, R151-50 1°"` (`str`) | Configures `expected regulation reference` under the exact parent path `entries[5]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[5].precheck_status` | `"MATERIAL_REVIEW_REQUIRED"` (`str`) | Configures `precheck status` under the exact parent path `entries[5]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[5].confidence` | `"HIGH"` (`str`) | Configures `confidence` under the exact parent path `entries[5]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[5].rationale` | `"The official code identifies a reserved-site planning mechanism that may materially affect a project and requires specific review."` (`str`) | Configures `rationale` under the exact parent path `entries[5]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[5].required_human_action` | `"Review the beneficiary, purpose, exact reservation, local planning documents, and project interaction with the competent authority."` (`str`) | Configures `required human action` under the exact parent path `entries[5]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[5].limitations` | `"This classification does not infer the reservation purpose from local text or determine whether a BESS project is authorized or prohibited."` (`str`) | Configures `limitations` under the exact parent path `entries[5]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[6].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `entries[6]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[6].type_code` | `"07"` (`str`) | Configures `type code` under the exact parent path `entries[6]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[6].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `entries[6]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[6].expected_official_label` | `"Patrimoine bâti, paysager ou éléments de paysages à protéger pour des motifs d'ordre culturel, historique, architectural ou écologique"` (`str`) | Configures `expected official label` under the exact parent path `entries[6]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[6].expected_legal_reference` | `"L151-19 et L151-23"` (`str`) | Configures `expected legal reference` under the exact parent path `entries[6]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[6].expected_regulation_reference` | `"R151-41 3° Et R151-43"` (`str`) | Configures `expected regulation reference` under the exact parent path `entries[6]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[6].precheck_status` | `"LIKELY_MATERIAL_CONSTRAINT"` (`str`) | Configures `precheck status` under the exact parent path `entries[6]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[6].confidence` | `"MEDIUM"` (`str`) | Configures `confidence` under the exact parent path `entries[6]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[6].rationale` | `"The official code identifies a broad heritage or landscape protection family likely to be material, while its exact local subject remains unspecified."` (`str`) | Configures `rationale` under the exact parent path `entries[6]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[6].required_human_action` | `"Review the protected element, local prescription, project siting and design, and competent-authority requirements."` (`str`) | Configures `required human action` under the exact parent path `entries[6]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[6].limitations` | `"The broad official category does not reveal the feature-specific protected subject or establish authorization or prohibition."` (`str`) | Configures `limitations` under the exact parent path `entries[6]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[7].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `entries[7]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[7].type_code` | `"07"` (`str`) | Configures `type code` under the exact parent path `entries[7]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[7].subtype_code` | `"04"` (`str`) | Configures `subtype code` under the exact parent path `entries[7]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[7].expected_official_label` | `"Éléments de paysage, (sites et secteurs) à préserver pour des motifs d'ordre écologique"` (`str`) | Configures `expected official label` under the exact parent path `entries[7]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[7].expected_legal_reference` | `"L151-23"` (`str`) | Configures `expected legal reference` under the exact parent path `entries[7]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[7].expected_regulation_reference` | `"R151-43 5°"` (`str`) | Configures `expected regulation reference` under the exact parent path `entries[7]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[7].precheck_status` | `"LIKELY_MATERIAL_CONSTRAINT"` (`str`) | Configures `precheck status` under the exact parent path `entries[7]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[7].confidence` | `"HIGH"` (`str`) | Configures `confidence` under the exact parent path `entries[7]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[7].rationale` | `"The official subtype identifies ecological landscape preservation likely to be material for a BESS project without meaning prohibited."` (`str`) | Configures `rationale` under the exact parent path `entries[7]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[7].required_human_action` | `"Review the exact preserved element, local prescription, ecological context, and project design with the competent authority."` (`str`) | Configures `required human action` under the exact parent path `entries[7]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[7].limitations` | `"This classification does not interpret the local preservation rule or determine project admissibility."` (`str`) | Configures `limitations` under the exact parent path `entries[7]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[8].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `entries[8]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[8].type_code` | `"15"` (`str`) | Configures `type code` under the exact parent path `entries[8]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[8].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `entries[8]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[8].expected_official_label` | `"Règles d’implantation des constructions"` (`str`) | Configures `expected official label` under the exact parent path `entries[8]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[8].expected_legal_reference` | `"L151-17 et L151-18"` (`str`) | Configures `expected legal reference` under the exact parent path `entries[8]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[8].expected_regulation_reference` | `"R151-39 dernier al."` (`str`) | Configures `expected regulation reference` under the exact parent path `entries[8]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[8].precheck_status` | `"DESIGN_REVIEW_REQUIRED"` (`str`) | Configures `precheck status` under the exact parent path `entries[8]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[8].confidence` | `"MEDIUM"` (`str`) | Configures `confidence` under the exact parent path `entries[8]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[8].rationale` | `"The official code primarily identifies construction-implantation rules relevant to project siting and design, while the local rule remains unread."` (`str`) | Configures `rationale` under the exact parent path `entries[8]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[8].required_human_action` | `"Review the exact local implantation rule against the proposed equipment layout and site design."` (`str`) | Configures `required human action` under the exact parent path `entries[8]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[8].limitations` | `"This classification does not interpret local setbacks, feature text, or project compliance."` (`str`) | Configures `limitations` under the exact parent path `entries[8]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[9].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `entries[9]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[9].type_code` | `"15"` (`str`) | Configures `type code` under the exact parent path `entries[9]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[9].subtype_code` | `"01"` (`str`) | Configures `subtype code` under the exact parent path `entries[9]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[9].expected_official_label` | `"Implantation des constructions par rapport aux voies et aux emprises publiques"` (`str`) | Configures `expected official label` under the exact parent path `entries[9]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[9].expected_legal_reference` | `"L151-17 et L151-18"` (`str`) | Configures `expected legal reference` under the exact parent path `entries[9]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[9].expected_regulation_reference` | `"R151-39"` (`str`) | Configures `expected regulation reference` under the exact parent path `entries[9]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[9].precheck_status` | `"DESIGN_REVIEW_REQUIRED"` (`str`) | Configures `precheck status` under the exact parent path `entries[9]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[9].confidence` | `"HIGH"` (`str`) | Configures `confidence` under the exact parent path `entries[9]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[9].rationale` | `"The official subtype specifically identifies construction siting relative to roads and public rights-of-way, requiring design review."` (`str`) | Configures `rationale` under the exact parent path `entries[9]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[9].required_human_action` | `"Review the exact local siting or setback rule against the proposed equipment layout and access design."` (`str`) | Configures `required human action` under the exact parent path `entries[9]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[9].limitations` | `"This classification does not interpret the local setback value or establish project compliance."` (`str`) | Configures `limitations` under the exact parent path `entries[9]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[10].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `entries[10]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[10].type_code` | `"17"` (`str`) | Configures `type code` under the exact parent path `entries[10]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[10].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `entries[10]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[10].expected_official_label` | `"Secteur à programme de logements mixité sociale en zone U et AU"` (`str`) | Configures `expected official label` under the exact parent path `entries[10]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[10].expected_legal_reference` | `"L151-15"` (`str`) | Configures `expected legal reference` under the exact parent path `entries[10]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[10].expected_regulation_reference` | `"R151-38 3°"` (`str`) | Configures `expected regulation reference` under the exact parent path `entries[10]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[10].precheck_status` | `"MATERIAL_REVIEW_REQUIRED"` (`str`) | Configures `precheck status` under the exact parent path `entries[10]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[10].confidence` | `"MEDIUM"` (`str`) | Configures `confidence` under the exact parent path `entries[10]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[10].rationale` | `"The official code identifies a social-housing-program planning mechanism that may materially affect land use and requires specific review."` (`str`) | Configures `rationale` under the exact parent path `entries[10]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[10].required_human_action` | `"Review the sector program, local planning provisions, land-use interaction, and authority requirements for the proposed project."` (`str`) | Configures `required human action` under the exact parent path `entries[10]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[10].limitations` | `"This classification does not infer the local program content or determine BESS authorization or prohibition."` (`str`) | Configures `limitations` under the exact parent path `entries[10]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[11].feature_family` | `"PRESCRIPTION"` (`str`) | Configures `feature family` under the exact parent path `entries[11]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[11].type_code` | `"18"` (`str`) | Configures `type code` under the exact parent path `entries[11]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[11].subtype_code` | `"00"` (`str`) | Configures `subtype code` under the exact parent path `entries[11]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[11].expected_official_label` | `"Périmètre comportant des orientations d’aménagement et de programmation (OAP)"` (`str`) | Configures `expected official label` under the exact parent path `entries[11]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[11].expected_legal_reference` | `"L151-6 et L151-7"` (`str`) | Configures `expected legal reference` under the exact parent path `entries[11]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[11].expected_regulation_reference` | `"R151-6 à R151-8-1"` (`str`) | Configures `expected regulation reference` under the exact parent path `entries[11]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[11].precheck_status` | `"MATERIAL_REVIEW_REQUIRED"` (`str`) | Configures `precheck status` under the exact parent path `entries[11]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[11].confidence` | `"HIGH"` (`str`) | Configures `confidence` under the exact parent path `entries[11]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[11].rationale` | `"The official code identifies an area governed by planning and development guidelines that may materially affect a project and requires specific review."` (`str`) | Configures `rationale` under the exact parent path `entries[11]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[11].required_human_action` | `"Review the applicable OAP text and graphics, project design interaction, and competent-authority requirements."` (`str`) | Configures `required human action` under the exact parent path `entries[11]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |
| `entries[11].limitations` | `"This classification does not interpret the local OAP or establish authorization, prohibition, or buildability."` (`str`) | Configures `limitations` under the exact parent path `entries[11]`. Consumers found by exact repository path reference: `tests/unit/test_bess_planning_feature_policy.py`. Exact allowed values/nullability are enforced by the consuming Pydantic/configuration validator. |

## 5. Classes / models / dataclasses

Not applicable; this file declares no Python class.

## 6. Functions and methods

Not applicable; this file declares no Python function or method.

## 7. Data contracts

The exact byte-bound values or text lines above are the data contract for this file. Structured validators in consuming Python modules remain authoritative for types, nullability, allowed values, units, source provenance, calculations, and downstream semantics.

## 8. Interfaces

Direct literal-path consumers are listed above. Git, uv, Python, configuration loaders, documentation readers, or generated-data directory conventions consume project metadata according to the file type.

## 9. Error handling

This passive file raises no exception. Its consumers reject missing, malformed, unsupported, duplicate, semantically invalid, or stale content with their documented controlled errors.

## 10. Side effects

The file itself has no runtime side effect. A consumer may read it, resolve dependencies, configure tools, or use it as source/policy evidence; those effects belong to the consuming function.

## 11. Security / trust boundaries

The SHA256 binds this documentation to exact bytes. Checked-in configuration identity is necessary but does not replace physical source/hash verification performed by source-complete adapters.

## 12. GIS / CRS rules

Only structured CRS fields listed above impose a GIS rule, and their consuming validators define it. Otherwise not applicable.

## 13. Provenance rules

Checked-in source locks, URLs, hashes, versions, profile IDs, and evidence references are textual provenance inputs. Their consuming code determines whether and how physical bytes are revalidated.

## 14. Business meaning

This file supports the `planning` domain only through its exact checked-in values and current consumers.

## 15. Explicit non-goals

- Planning facts and prechecks do not constitute legal advice, authorization, or prohibition.

## 16. Tests

Tests that load or mention this path are documented in their companion files. No test is inferred solely from the filename.

## 17. Change impact

Changing these bytes requires reviewing every consuming validator, source/policy/config hash, generated result or artifact lineage, affected tests, and this companion SHA256. Dependency-lock changes also require `uv lock --check` and `uv pip check`.
