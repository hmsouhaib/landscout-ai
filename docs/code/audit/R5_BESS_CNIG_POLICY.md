# DOCS.CONTINUITY.1.R5 — BESS CNIG policy configuration fidelity

Status: bounded two-row implementation; validation/publication evidence below.
Global audit **PARTIAL**; independent R5 review **PENDING**.
No application, YAML, test, legal or business-policy approval.

## Authority, readiness and successor approval

Authority: [exact R5 ticket](../../project/tickets/DOCS.CONTINUITY.1.R5.txt),
attachment `80450339-268a-4ff6-b715-a24e8b270722/pasted-text.txt`.
Archived raw bytes SHA256:
`d9d2586099719101b2eb1444e2be39451b4df05137efa0f5a3c5b51275d711d5`.
Section 1 is a supplied ChatGPT read-only reviewer statement, not a new Codex
execution or unavailable cryptographic signature.

Starting root `C:/souhaib/landscout-ai`, clean branch
`recovery/docs-continuity-1-partial`. HEAD, local tracking recovery and actual
remote recovery: `ab7d9d41fe2444b63d31c7dc075af59cab8884f7`.
Local/actual remote main: `aa4ebc7063f2abdfcbae95154ba89b5b1a0dba02`.
No unmerged index entries or Git-operation markers.
Sandbox ls-remote failed to connect to GitHub:443; native explicit-root Git
then reproduced the known ownership mismatch (repository owner CodexSandboxOffline,
native caller souha). Only then the authorized command-scoped exception succeeded:

```text
git -c safe.directory=C:/souhaib/landscout-ai ls-remote --heads origin main recovery/docs-continuity-1-partial
```

No fetch, persistent trust/ACL/security/dependency change or destructive recovery.
The supplied R4 approval covers documentary R4 only at the starting SHA, parent
`e89f7f176f7bc3369977055aceb6423b882d3429`; no R4.1 requested.
[CURRENT_STATE](../../project/CURRENT_STATE.md) records the successor verdict and
its limits, linking the exact statement. The unchanged [R4 receipt](R4_CNIG_CONFIGURATION.md)
retains its historical PENDING status, local checker evidence and candidate/final
distinction. Independent isolated GitHub/PyYAML recomputation is not application
loader/Pydantic/pytest/auditor/source execution. No Windows checkout, visual or
legal approval is inferred. Acceptance of R4 adds no new closure count.

## Ownership and original-scope progress

The original [matrix](recovery_file_matrix_2026-09-17.json) and all six
[review fragments](reviews/planning.json) were queried by exact path. Both R5
records occur only in planning.json; neither has an extension counterpart.
Historical “transferred_to: inpn_roads extension” was not materialized. Retain
planning ownership and the original status/read/hash/contract/transfer evidence
under r5_prior_evidence. No other row/top-level finding changes.

| Original path | Initial status/read_complete | Final status/read_complete | Symbols |
|---|---|---|---|
| configs/planning/muret_bess_cnig_feature_policy.yaml | NOT_READ / false | CHECKED / true | 0 |
| docs/code/files/configs/planning/muret_bess_cnig_feature_policy.yaml.md | NOT_READ / false | CORRECTED / true | 0 |

Starting companion SHA256:
`68f27bc57f35bdcfa508fd56bb83374fffc131970fa2aa9c25789b0ff013f40a`.
Final companion SHA256:
`630da631d3b803d09e9a1e78e546fd51ef2bf8e0c484a31f2ad083c87b36ce61`.
YAML SHA256 unchanged:
`8a26fcb8ee7e2f028baca65d94c2b5be5445ba9fc4b82b9b1a21843f933f8b2a`;
Git blob `b39ed76230fd5b55e3142b031e24d5adbb762121`.

Deduplicated original scope: **73 + 2 = 75/245 files** and
**1,594/4,769 symbols**; **170 files / 3,175 symbols unfinished**.
File totals CHECKED 28, CORRECTED 47, READ 141, NOT_READ 29.
Symbols CHECKED 1,593, CORRECTED 1, READ 2,078, NOT_READ 1,097.
Current Git enumeration and new ticket/receipt are not the original denominator.
No loader/compiler/test/downstream companion is closed merely because read.

## Actual reading and corrections

Read AGENTS, WORKING_RULES, RESUME, CURRENT_STATE, relevant step/decision/backlog
records, technical README/architecture/data-flow/planning route, audit ledger and
R4 receipt. Read the entire 154-line YAML and every unique prose/table/consumer
section of the old 930-line companion. Generic repeated boilerplate was displayed
by unique text and all 151 leaf rows inspected; repeated Python/YAML/Base64 blocks
were compared mechanically to already-read source. Four embedded class snapshots
matched; BessPlanningFeaturePolicyConfig did not: the old declaration/table still
said dict rather than Mapping and lacked the current frozen-mapping/serializer
contract. The YAML readable/Base64 snapshots both reproduced all 10,147 source
bytes; they remain complete in the replacement companion.

Dependency read ranges in unchanged input files (reading, not unit closure):

- `src/landscout/stages/bess_planning_feature_policy.py` **1–1114**, complete:
  constants/models/loader, shape and identity checks, dictionary completeness,
  propagation, component/result hashes, envelope/artifact/full validation and
  compiler error order.
- `src/landscout/common/strict_yaml.py` **1–60** and
  `src/landscout/common/immutable_mapping.py` **1–170**, complete. The generic
  recursive freeze helper is not confused with the separate canonical-JSON
  leaf validator; priority keys/values here are already typed strings/integers.
- `src/landscout/stages/resolve_planning_feature_codes.py` **93–309, 342–442,
  550–578, 818–970, 1064–1128**: profile/record/hash/load/reconstruction,
  nullable dictionary checks, dictionary construction, complete-result commitment,
  source-complete builder and validator. Other module portions are not closed.
- `src/landscout/stages/apply_bess_planning_feature_policy.py` **442–574**:
  exact lookup, copied decision fields, unresolved-code distinction, no text fallback.
- `src/landscout/stages/aggregate_bess_planning_feature_policy.py` **73–94,
  714–874**: controlling/contextual relation sets and maximum-priority/lowest-confidence
  selection, with unresolved controlling codes preventing selection. These are
  downstream uses, not output of the policy compiler.
- `tests/unit/test_bess_planning_feature_policy.py` **1–1242**, complete:
  all fixtures/helpers/branches/assertions, including artifact tests.
- `tests/unit/test_resolve_planning_feature_codes.py` **1–338, 537–671**:
  imported synthetic physical GPKG/inventory/manifest/config/intersection helpers.
  Additional displayed context 339–347, 500–536 and 672–675 was not audited.
- `tests/unit/test_deep_immutability.py` **1–113, 300–482** and
  `tests/unit/test_strict_serialization.py` **1–23**: relevant imports/helpers,
  loaded BESS graph/hash checks versus generic AOI/structure/road mutation/alias
  cases, and YAML decoding assertions.

The [corrected companion](../files/configs/planning/muret_bess_cnig_feature_policy.yaml.md)
explains all ten root fields, seven locks, five priorities and all eleven fields
of every entry. Required-nullable references, exact codes/text, forbidden flags,
source-version shape versus equality, config immutability versus mutable result
DataFrame, guard order, intrinsic versus complete validation, all three config
identities and output-hash influence are distinguished. There are **151 leaves,
12 entries (4 INFORMATION / 8 PRESCRIPTION), 22 textual reference cells and two
true nulls**. All 36 narrative strings remain literal policy values.

| Status | Priority | Entries |
|---|---|---|
| LIKELY_MATERIAL_CONSTRAINT | 50 | 3 |
| UNKNOWN | 40 | 1 |
| MATERIAL_REVIEW_REQUIRED | 30 | 3 |
| DESIGN_REVIEW_REQUIRED | 20 | 2 |
| CONTEXT_REVIEW_REQUIRED | 10 | 3 |

Confidence: HIGH 8, MEDIUM 3, LOW 1. No status is a permit/prohibition, HIGH is not
a measured probability, and priority is not a score or automatic rejection.
UNKNOWN as a configured official meaning is separate from an unresolved code pair.

## Test evidence boundaries — no pytest

All tests above were read, **none executed in R5**. The checked-in twelve-entry
snapshot helper replaces a synthetic coded result's locks/dictionary then invokes
private _build_result; it does not prove the public compiler accepted real Muret
sources. _compiled_fixture instead uses a four-meaning synthetic policy and actual
temporary GPKG reads/intersections with synthetic archive metadata, not acquisition.
Missing/extra/meaning/order changes generally recompute declared entry hashes;
the explicit corrupt in-memory digest test does not. Rehashed rationale/table
mutations test reconstruction, not immediate DataFrame immutability. The
bool-priority envelope branch changes dtype to object and can fail at schema
before row validation. The snapshot “immutable” name is not a mutation assertion;
a separate priority assignment test is. Generic alias tests target AOI/structure,
not BESS-specific aliases. These limits are not automatic new backlog findings.

A-001..A-004, the five historical test-evidence limitations and R3-T01..04 remain
unchanged/open according to their existing records. A-004's relevant optional-text
versus literal-null-result boundary is described without a new reproduction or
duplicate finding. Current YAML uses true nulls. No new concrete application
finding was established; BACKLOG_AND_GAPS remains byte-identical.

## One bounded offline execution

Existing installed environment only. Exact PowerShell command below; one call to
each existing loader, **policy load once**, comparison entirely in memory.
The audit hook rejects DNS/connection socket events, allowing socket construction
and gethostname used during imports. No forbidden event occurred. No production
artifact, test fixture, cache/source file or compiled policy result was created.

```powershell
@'
import sys, json, time, hashlib, collections, importlib
from pathlib import Path
sys.dont_write_bytecode=True
blocked=[]
def offline(event,args):
    if event.startswith('socket.') and event not in ('socket.__new__','socket.gethostname'):
        blocked.append(event)
        raise RuntimeError('R5 offline guard: '+event)
sys.addaudithook(offline)
p=importlib.import_module('landscout.stages.bess_planning_feature_policy')
c=importlib.import_module('landscout.stages.resolve_planning_feature_codes')
path=Path('configs/planning/muret_bess_cnig_feature_policy.yaml')
start=time.perf_counter()
config=p.load_bess_planning_feature_policy_config(path)
seconds=time.perf_counter()-start
profile=c.load_cnig_feature_code_profile(Path('configs/planning/cnig_plu_2017_feature_codes.yaml'))
a={(e.feature_family,e.type_code,e.subtype_code):(e.expected_official_label,e.expected_legal_reference,e.expected_regulation_reference) for e in config.entries}
b={(r.feature_family,r.type_code,r.subtype_code):(r.official_label,r.legal_reference,r.regulation_or_annex_reference) for r in profile.records}
assert a==b and c._profile_sha256(profile)==config.source_lock.cnig_profile_sha256
payload=config.model_dump(mode='json')
def leaves(x):
    return sum(map(leaves,x.values())) if isinstance(x,dict) else sum(map(leaves,x)) if isinstance(x,list) else 1
print(json.dumps({'policy_load_calls':1,'cnig_load_calls':1,'loader_seconds':seconds,'network_attempts':blocked,'bytes':len(path.read_bytes()),'lines':len(path.read_text(encoding='utf8').splitlines()),'raw_sha256':hashlib.sha256(path.read_bytes()).hexdigest(),'entries_sha256':p._policy_entries_sha256(config.entries),'policy_sha256':p._policy_sha256(config),'cnig_sha256':c._profile_sha256(profile),'root_fields':len(payload),'lock_fields':len(payload['source_lock']),'priority_keys':len(payload['status_priority']),'entry_fields':sorted(set(len(e) for e in payload['entries'])),'leaf_count':leaves(payload),'families':dict(collections.Counter(e.feature_family for e in config.entries)),'statuses':dict(collections.Counter(e.precheck_status for e in config.entries)),'confidences':dict(collections.Counter(e.confidence for e in config.entries)),'true_null_references':sum(v is None for e in config.entries for v in (e.expected_legal_reference,e.expected_regulation_reference)),'types':{'config':type(config).__name__,'lock':type(config.source_lock).__name__,'entries':type(config.entries).__name__,'entry':type(config.entries[0]).__name__,'priorities':type(config.status_priority).__name__,'priority_values':sorted(set(type(v).__name__ for v in config.status_priority.values()))},'comparison':'12 exact triples, 36 text/null cells, full CNIG profile identity equal','payload':payload},ensure_ascii=False,indent=2))
'@ | .venv\Scripts\python.exe -B -X utf8 -
```

Exit **0**; no exception. Policy-loader elapsed time **0.014085800001339521 s**;
tool process wall time 1.0086163 s includes imports/printing. Runtime config
BessPlanningFeaturePolicyConfig, lock PolicySourceLock, entries tuple of
PolicyEntry, priorities FrozenDict with int values. The 12 exact triples and all
36 label/reference-or-null cells equal the unchanged loaded CNIG profile.

| Calculated identity | SHA256 |
|---|---|
| Raw YAML bytes | 8a26fcb8ee7e2f028baca65d94c2b5be5445ba9fc4b82b9b1a21843f933f8b2a |
| Ordered policy entries | 1d3e63f1123000402065b74402cb1e2295db2ac5655209ce410aaf36bfc2be91 |
| Complete validated policy model | 1cfca0eb3d777e9b6604748e8a81609abe7b728de8d0695711cd569180df6489 |
| Complete CNIG profile, equal to policy lock | 5611b814eb4bc057578b908c6505094f9df5d2c2bf4ca126629b1362983c47ee |

The configured archive SHA, document ID and complete CNIG-result SHA remain locks,
not freshly reconstructed evidence. No full compiler/result builder, pytest,
GPU/EP pipeline, GIS computation, network acquisition, legal research or artifact
persistence ran. Tests' two output-hash constants are described as test evidence
only, not recalculated R5 outputs.

## Bounded preservation and document checks

The scoped read-only checker ran from stdin with the existing Python environment;
no new checker file was added to Git. Exit **0** on working bytes:
**106 protected files** match their authority manifest's Git/raw-checkout hashes;
all **282 out-of-scope paths among 286 starting paths** match the starting Git
and checkout evidence. That includes all prior tickets/receipts, the approved R4
companion, YAML/source/tests, auditor/test, dependencies and global coverage/matrix.
Exactly six allowed paths change (four existing paths plus this receipt/ticket).

Two historical checkout-vs-Git EOL exceptions remain exact, not repaired:
`configs/planning/muret_bess_zoning_policy.yaml` raw
`879d50627c063bb10096950d004cf4d4e446ff04ef9a1178b3e3fb28e2ffdae3` versus Git
`c736ea8901997f4852fd1f72a3f6f34282452f18dcd9abc0ccbce8255adaad45`;
`docs/project/tickets/DOCS.CONTINUITY.1.RESUME.2026-09-17.txt` raw
`9e18c25d571c5cfe34391d1f35833634bb4c3672087781360752fa0fba7243ed` versus Git
`b5a0fa80569fbc0b11d28718c0037f9e8ba23caac5dadca6fd60c05c7cc64a5b`.
Only those two normalized-to-LF comparisons are exceptions to byte equality.

Strict JSON decoding rejects duplicate keys/nonfinite constants; only the two
owner rows differ, their original evidence is retained, and current source/
companion fingerprints match. Reconciliation resolves each of 245 original paths
through its actual fragment/embedded record and deduplicates symbols by path,
kind and qualified_name: 4,769 identities, counts as above. No status inferred
from AST enumeration or READ.

Both old/current readable YAML and Base64 snapshots reproduce all 10,147 bytes.
All **151 table leaf values**, including every **132 entry cells**, match the
unchanged parsed YAML; five displayed public signatures match source.
The scoped changed-Markdown checks found **6 explicit ID occurrences outside
fences, all unique**, **56 valid local links** and **132 valid table lines**.
Whole starting-Markdown incoming-link scan found **zero anchored links targeting
this companion**; existing section/model headings are retained where applicable.
Fences are balanced. Those counts describe this pre-INDEX receipt content;
late receipt links/lines are checked separately.

Cumulative whitespace output against preserved main is byte-identical to the
starting output: **13 inherited observations**, twelve cold-start hard-break
lines and the original continuity-ticket blank EOF. No archive rewriting.
Working and staged delta checks are recorded with the INDEX candidate below.

Renderer discovery: pandoc/markdown/marked/node not on PATH; installed Python
has no markdown/markdown_it/mistune/docutils. No usable renderer was exercised,
no dependency installed or content uploaded. Visual rendering remains **PENDING**;
syntax/link/table checks are not a visual pass.

## Staged INDEX auditor

The six explicit allowed paths were staged; INDEX-scoped checks exited **0**,
with the same 106/282 preservation comparisons, two-row scope, 151 leaf values,
snapshots, five signatures, 6 unique IDs, 56 local links and 132 table lines.
New working and staged `git diff --check` both exited **0**.
The unchanged auditor ran **once**, after saving the candidate manifest via the
already-existing external r31_check.py --manifest helper (index enumeration only,
not another full audit):

```text
.venv\Scripts\python.exe -B -X utf8 tools/audit_documentation.py --check --progress > C:\souhaib\r5-candidate.stdout.txt
```

**Process exit 1**, `completed=true`, **10,266 findings**.
This is a completed PARTIAL check, not exit 2/interruption/global acceptance.
Exact candidate manifest SHA256:
`bf86629b374ddb36d2230feb0626eb58eb988e30e8693f50899bb6c122a94c45`.
**288 files, 285 unique blobs, 29,080,474 per-path content bytes**;
94 Python files / 94 AST parses / 4,863 enumerated symbols.
Original coverage remains 245 files / 4,769 symbols. Five Git processes,
non-shallow history, unchanged index postcondition.

Actual phase seconds: index 0.5664855; coverage-input 0.0337954;
Python 2.4081504; checkout-Markdown 0.8345792; coverage 0.3745828;
references 0.3419263; history 0.2039037; index-postcondition 0.0788644.
The enclosing manifest-capture-plus-auditor shell took 7.0501144 s including
launcher/output overhead; that is not the auditor-only elapsed time or a benchmark.
Verbose progress display was truncated by the tool budget; terminal metrics and
exit were returned, and complete stdout was separately read/counted.

Actual findings: **5,035 coverage/staleness**, **215 mechanical**,
**5,016 pending recorded acceptance**. Detailed prefix counts:
missing documented anchors 4,769; unresolved symbol reviews 4,769;
unresolved semantic rows 245; missing companion exceptions 131;
stale file fingerprints 110; ambiguous companion bases 100;
unresolved qualified references 100; export inventory mismatches 24;
missing exact source snapshots 8; bad local links 3; DEV_LOG diagnostics 2;
inventory mismatch, semantic-ledger-not-COMPLETE, stale companion hash,
unfinished history audit and raw-checkout/EOL diagnostic one each.

Counter comparison with the retained R4 stdout finds exactly: removal of this
YAML's ambiguous-basis diagnostic; addition of this changed companion's stale
fingerprint in the intentionally unmerged original ledger; replacement of the
inventory line to include the new R5 ticket. All other findings unchanged.
No new bad local link, unresolved qualified reference or unexpected anomaly.
No global coverage promotion, suppression or checker modification was used to
obtain a clean verdict.

Full stdout outside Git, PowerShell UTF-16:
`C:\souhaib\r5-candidate.stdout.txt`, **2,315,742 bytes**, SHA256
`e6304db38284e9f10fadf88b313f513f59c4c07b0826fecfaa3f58e608e880bc`.
Sorted exact path/mode/blob entries:
`C:\souhaib\r5-candidate.manifest.json` (PowerShell UTF-16).
Compact ASCII-safe JSON of that list, without terminal newline, reproduces the
candidate manifest digest above.

**Only this receipt is completed after the full run.** Its subsequent bytes are
not retroactively part of that candidate. Separate final scoped checks cover the
exact one-path manifest delta, final receipt links/fences/bytes, original six-path
scope, all protected/out-of-scope identities and current owner/snapshot bindings.
No second full auditor, loader, application or test run. The thirteen historical
whitespace observations remain separate from the clean new staged delta.

## Stop and next queue item

Authorized publication only on recovery with
`docs: audit BESS CNIG policy configuration reference`. Resolve final commit SHA
through Git after committing, verify clean tree and local/tracking/actual recovery
agreement plus unchanged main; no future self-SHA written into this receipt.
Next actual sorted NOT_READ original path:
`configs/planning/muret_bess_zoning_policy.yaml` — identified, **not started**.
Stop for independent R5 review. Global audit/cold-start acceptance/rendering/final
full validation and 7F.1C.1 exhaustive semantic review remain pending.
